# Keep the TownRoomManager source marker semantic instead of ILSpy-format-specific.
checker = Path('tools/ci/check_town_movein_schedule_source.py')
text = checker.read_text(encoding='utf-8')
old = '        "_roomLocationPairs.Add(Tuple.Create(npcID, pt))"\n'
new = '        "_roomLocationPairs.Add("\n'
if text.count(old) != 1:
    raise SystemExit('TownRoomManager SetRoom marker anchor drifted')
checker.write_text(text.replace(old, new, 1), encoding='utf-8')

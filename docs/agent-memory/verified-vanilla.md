# Verified vanilla facts

2026-09-09 whole ordinary Dungeon: original executable comparisons24 independent flat+16 identical-real-prefix+14 extraSmallseeds match all16-byte cells, ordered chest items/stacks/prefixes, OldMan anchor and sharedRNG. CheckChand framing/destruction144 matches; each destroyed cell consumes10Next2 dust draws despite suppressed dust. MakeDungeon_GetEntranceSettings consumes a shared seed before overwriting it. Ordinary DungeonEntranceHasStairs=true; Legacy/Dome/Tower invoke clean-room stairs. MakeDungeon crackedSolidityfalse reaches wall propagation, CanHit and both chest supports. PlaceTile137 reframes neighbors AFTER installation. Legacy layout aliases outerProgressionBounds0 to mutable dungeonBounds (entrance updates/postchest inflation retained). Named retained regressions and negative counts in work-state. Bounded R16, not preceding-prefix/all-GenVars/special-seed/final-world equality.

## Whole Dungeon completion WIP — 2026-09-09

- Actual official executable: 216 total complete tile-array/shared-RNG fixtures across DungeonGlobalBookshelves, DungeonGlobalLights and DungeonGlobalBanners pass. Bookshelf random book choice precedes corridor-length admission; book PlaceTile consumes its frame draw before the Water Bolt roll. Lights include raw-type proximity checks, unconditional lantern-style roll, direct placement preserving metadata, source Collision.CanHit switch query and alternating-axis wiring. Banners use source retries/solid ceiling/separation. Artifacts and retained tests in current work-state; not a complete Dungeon proof.
- Original global sampling-bound metadata checked for 324 buildings,18 layouts,54 precalculated routes. This differs from protected component bounds: Legacy replaces Top and adds first roof strip, Dome updates only admitted initial Slime cells, Tower does not add bounds; zero-brush routes do not update global bounds.
- Correct earlier chest assumption: WorldGen.genRand is Main.rand (WorldGen.cs getter); Item.Prefix(int,out bool) uses WorldGen.genRand while generating/loading. Omitting natural prefixes changes the worldgen random stream. Existing ChestLoot1458 implementation/comment still need correction, including Dungeon key/rune side state and full generated-chest evidence.

## Dungeon spikes / wall variants — 2026-09-09

- Local acceptance: Release6480/6480,183.142s,zero warnings/errors; WindowsNativeAOT/sixsmokes/fresh dual-server load and unchanged reference budgets pass. All144 original-executable comparisons repeated on final DLL. Full-world tileL1 .133518/wallL1 .393741 remain nonzero; no E9 promotion. LinuxNativeAOT/client playthrough unverified; exact hashes/artifacts and cleanup in work-state.

- DungeonGlobalSpikes ordinary globalFeatureScalar1: target(int)(42f*(width/4200f)); no forced minimum1. Shared retries accumulate across successes and increment completed only after attempt1001. Vertical supports scanned by activity, horizontal scan stops at5tileedge; support rejects Main.tileFrameImportant/tileCut/current isCrackedBrick, not all nonsolid cells. Initial check is asymmetric; runs preserve source left/up then right/down Next(5,13) order. Base only changes type48; tips also clear shape/setactive, preserve liquid/paint/wires/frames. Check feature eligibility only at base and first tip, as source, not arbitrary extra gates. Dome/Tower entrances reject spikes; ordinary rooms admit, wall350/margin5 remain gates.
- DungeonGlobalWallVariants:5rounds*3variants; radiusNext(40,240), X/Y sampled with exclusive Right/Bottom; radius*0.4f stays fractional, xouter/yinner, seeding only below surface. Seeds invoke SpreadWallDungeon, not circular repaint. Flood can reach above surface/outside seed circle, only base unsafe wall eligible; active full non-actuated solid cells recolor but stop propagation, halfbrick/slopes/actuation/nonsolid continue. Only wall changes. Existing other variants, walls0/62/244/350 stop. Ordinary same-palette entrance and room styles admit walls; no special-seed styles assumed.
- New queue uses changed wall as visit mark; eligibility independent of neighbor wall/order, each accepted cell enqueued at most once. Original executable SHA4B87890AC53D40F61DB5F928693A379ACF4CCBD8ED3B47EB32FB096F145DF034 actual feature144fixtures match all16-byte normalized cells/nextRNG. Retained151/Dungeon1467 green;124negative failures restored. Upstream graph sampling-envelope/complete crawler still unverified; no E9 promotion.

## Dungeon candidate discovery / doors — 2026-09-09

- Local acceptance: Release6329/6329,183.403s,zero warnings/errors; WindowsNativeAOT/sixsmokes/fresh dual-server load and unchanged reference budgets pass. Full-world tileL1 .133149/wallL1 .395951 are still nonzero; no E9 promotion. LinuxNativeAOT/client playthrough not exercised. Artifacts, hashes and process cleanup are recorded in work-state.

- Source DungeonUtils.CalculatePlatformsAndDoorsOnEdgesOfRoom interleaves top/bottom per x and left/right per y, checks only inactivity (not matching dungeon wall), preserves duplicates; ordinary room fluff3 versus hall fluff10door/5platform. Original InnerBounds metadata uses painted max-1 even though Rectangle is exclusive. Ordinary GetCurrentDungeonStyle has EMPTY DoorItemTypes/PlatformItemTypes: platform style-1 consumes no RNG. Crawler scans rooms then halls after entrance candidates.
- LegacyDungeonHall.StartDirection/EndDirection are the same retained axis, NOT sloped movement velocity or endpoint slope. Candidate positions use integer truncation already retained by existing component. AddExtraPlatformsIfNeeded returns for all non-DualDungeon worlds. No ordinary intermediate platforms should be invented.
- DungeonGlobalDoors: style draw BEFORE failed search; width scan asymmetrical [-fluff,+fluff-1], direction0 first strict minimum gap, -1 rightmost, +1 leftmost; gap<20, door neighborhood/space gates, masonry/ordered random side supports, source ClearEverything vs slope-only resets. Reuses established inactive-anchor PlaceTile door framing. Protected wall350/support gates preserved; unsupported frame-important object destruction not approximated.
- Official executable SHA4B87890AC53D40F61DB5F928693A379ACF4CCBD8ED3B47EB32FB096F145DF034:234 door fixtures +8 candidate suites pass all cells/ordered metadata/next RNG (.cache/dungeon-doors-oracle.log);250 retained new tests, focused1316. Negative controls30wrong ties/2old inset fail then restored. This is ordinary synthetic component proof, NOT full Dungeon/whole-world E9.

## Cohesive Dungeon buildings/platforms — 2026-09-09 local acceptance

Final standard Release6079/6079,180.596s,explicit child exit0; solution0warnings/errors, WindowsNativeAOT/sixsmokes/fresh dual-server loading and unchanged comparison budgets pass. Source/doc/domain/projectgraph/diff checks pass. Whole-world tileL1 .133650/wallL1 .429207 remain nonzero. LinuxNativeAOT/officialGUIclient unverified; no full-prefix promotion.

- Actual original Linux1.4.5.8 executable SHA4B87890AC53D40F61DB5F928693A379ACF4CCBD8ED3B47EB32FB096F145DF034: Dome108 and Tower108 complete buildings match normalized16-byte cells,nextsharedRNG,OldMan,bounds and ordered platform positions/flags. After actual DungeonGlobalPlatforms, both108 continuation sets also match. Separate ordinary platforms30 and pillar/wedge/mosaic24 match. Original ContentSamples/ItemID postsetup and tree networking buffers initialized normally; no patched game algorithms. Retained tests added; old rectangle negative fails216/222, restored. Final full/native/fresh gates in work-state, not implied by component proof.
- Source pinned: Dome/TowerEntrance, DungeonWindowBasic/Mosaic, DungeonPillar, DungeonUtils.BottomWedge, DungeonGlobalPlatforms, Place3x2/PlacePot/PlaceOnTable1x1/TileFrame/RangeFrame/GrowTree. WindowPlatformItemTypes length1 still drawsNext1. Pillar auto-height scans activity; smoothing precedes actuation. Tower PlaceTile215 resets non-frame-important anchor half-brick/frames and frames inactive neighbors even when placement fails. GrowTree(ignoreWalls=true) bypasses only wall eligibility; finalRangeFrame cleans inactive block and absent-wall metadata.
- Platform placement searches source height fluff, respects nearby platforms/clearance, forced candidates bypass those gates, paints from center left then center+1 right and keeps duplicate candidate ordering. Shelf RNG tests pots before potion bottles before books; NoWaterbolt suppresses its roll. Preserve the original book check [placeY,placeY], not an invented corrected coordinate. Unsupported shaped/liquid platform or pre-existing object framing remains fail-closed. These proofs do not cover all global features/candidate discovery or a complete Dungeon/world prefix; ledger unchanged.

Latest layout-origin gates: Release5586/5586,184.682s,build0warnings/errors,WindowsNativeAOT+sixsmokes, freshSmallClassicCorruption1458 dual-server loading and docs/source/domain/projectgraph/diff pass. Source anchor now matches official delta(0,0) on that ordinary Legacy seed; whole-world tileL1 .134359/wallL1 .433354 remain unequal. Detailed artifacts/resume in work-state. No LinuxNativeAOT/client acceptance claimed.

## Dungeon layout origin / room interior metadata — 2026-09-09

- Verified complete LegacyDungeonLayoutProvider, LegacyDungeonRoom, DungeonBounds and relevant DungeonCrawler.MakeDungeon 1.4.5.8. Crawler draws four entrance strengths and width/60+Next(base/3) steps first. PrecalculateEntrancePosition then replaces cursor with entranceX-10+Next20,entranceY+30 BEFORE initial hall-settings/room-settings/starting-room seeds. Ordinary origin has no extra random draw. Existing decrement/unconditional Next3/branch/final-room loop retained.
- DungeonCrawler uses stable minimum ROOM InnerBounds.Top, then integer InnerBounds.Center.X and Top for connection. It does not choose outer shell or add4. DungeonBounds initializes from integer StartPosition, clamps10..dimension-10 and expands zero width/height through the same clamp; each room inner brush incorporates x0,y0,x1-1,y1-1. Painting still clamps0..dimension-1 with exclusive end. Existing graph Bounds remains painted extent, not a claim of source OuterBounds metadata equality.
- Actual official Linux executable SHA4B87890AC53D40F61DB5F928693A379ACF4CCBD8ED3B47EB32FB096F145DF034 invoked via ignored oracle, no copied bodies.18 whole-layout fixtures:1000x800 ground,seed42/1458/8675309,ordinary/precalculated,left/right origins,all3palettes,steps0/20/35. Full normalized cells,next sharedRNG,cursor,direction,ordered roomStart/End/InnerBounds,selected anchor all match. Existing63 room fixtures additionally match metadata including world edges;9 explicit interior/boundary goldens retained. Dungeon574pass; old origins/shell negative20/22fail, restored.
- Proof covers layout on controlled inputs, not complete Crawler or world prefix. Source Dome/Tower buildings, remaining setup/decorations and full Dungeon equality remain open; no audit promotion. Current integration gates in work-state.

## Live falling blocks / autonomous ordinary-ore Mining — 2026-09-09

- WorldGen.SpawnFallingBlockProjectile/GetSandfallProjData: admitted tiles53,112,116,123,224,234,495 map to projectiles31,56,67,71,179,241,812, source damage10/knockback0. Dedicated spawn center(tileX*16+8,tileY*16+10),10x10 body, initial vy.5, ai0=0. Actual SetDefaults: ai10,timeLeft3600,penetrate-1,friendly+hostile,tileCollide,ignoreWater=false,extraUpdates0. SetDefaults knockback6 is overwritten by NewProjectile's supplied0. Coin411..414 have different noncombat/penetration/collision semantics and remain excluded.
- Source BlockBelowMakesSandFall and AllowsSandfall: air/actuated below, non-solid below with empty/actuated second cell, or stalactite165; protected above BasicChest21/467,Fake441/468,323/88/80/77/26/475/470/597. AI_010 adds.41 to vy, clamps10; first owner255 ai0=0 update disables tile collision and changes ai0=1. HandleMovement uses non-fallthrough platforms. AI10 wet motion uses pre-AI wetVelocity, not post-AI/water damping. Collision Kill sees collision-clamped position before common UpdatePosition tail. Halfbrick descending landing may decrement y; rail314/conveyors421/422 and unsupported landing generate an item. Admitted item169/370/408/424/1103/1246 are12x12; shell4090 uses DefaultToPlaceableTile14x14.
- Tile.ClearTile is NOT KillTile or ClearTileAndPaint: only ClearSlope,active(false),inActive(false); type/frame, paint/coatings, actuator, wires, walls, liquids remain. Added this semantic operation to the SAME mutation owner. Actual official ClearTile tested with all4 liquid kinds and all known flags. Old KillTile production detachment fails retained metadata test. Other ordinary frame canonicalization policy is unchanged.
- Independent oracle uses actual original Linux TerrariaServer1.4.5.8 SHA4B87890AC53D40F61DB5F928693A379ACF4CCBD8ED3B47EB32FB096F145DF034, normally initialized Main.player/projectile/NPC/WorldItem/dust arrays and NetMessage/Netplay recipients, no skipped Kill/AI. `.cache/falling-clear-reference.log`:28 motion/landing fixtures +4 metadata fixtures pass. Dry Y ticks1/2/3/20:325.91,327.23,328.96002,421.10004; wet325.5,326.41,327.73,412.90002. Dry/platform/half all land at row39 above floor40. Binary post-Kill position is intentionally not the terrain landing position. Source AI decoration cutting/top-slope side effects beyond admitted landing and complete town-NPC interactions remain open.
- Player.GetPickaxeDamage/Item.SetDefaults: Vortex2776 pick225,useTime6,useAnimation12 completes each admitted ordinary ore/Dirt/Stone in one swing. Ore tile/item map7/12,166/699,6/11,167/700,9/14,168/701,8/13,169/702; all ore items12x12,maxStack9999. Bot12-tick full-animation cadence and search/reach/structure policies are runtime policy, not guessed vanilla equipment modifiers. Mining still materializes exact world drops then uses normal pickup; no direct resource grant. Hostile falling damage has an environmental projectile source, not a fictitious NPC; existing combat handles defense/immunity/GodMode/death and source ByProjectile(-1,slot) packet118 flags20/type.

## Precalculated entrance connection — 2026-09-09

- Actual DungeonCrawler.MakeDungeon_GenerateNextEntranceHall_Precalculated initializes amountPassed to integer distance, interpolates current -> entrance by amountPassed/distance, samples Next(10,30) then clamps against distance-amountPassed (minimum1), takes shared Next seed, then decrements countdown. First segment is nearly the entire connection, not a short advance. Fractional distance below1 consumes draws but paints no cells. Main entrance loop permits99 attempts, not100 plus an invented catch-up.
- LegacyEntranceDungeonHall still consumes initial sampled strength/velocity draws for CalculateHall/GenerateHall at(0,0). Nonzero override endpoints replace direction and set brush steps to ceil(endpoint distance / normalized length), overriding OverrideSteps. Preserve double reciprocal normalization and omit nonprecalculated surface probing/damping. Platforms recorded every10 painted steps, including first: InAHallway=true,PlacePotsChance=.25; duplicates/order matter. GenerateHall resets final bounds to source-clamped start..start+1 after CalculateHall has set Processed; final bounds are not painted extent.
- Independent original Linux1.4.5.8 binary SHA4B87890AC53D40F61DB5F928693A379ACF4CCBD8ED3B47EB32FB096F145DF034:54 complete-route fixtures match all16-byte cells, EACH step's double position/countdown/integer cursor/bounds, ordered platforms and next shared RNG. Six inputs x three palettes x three seeds42/1458/8675309; include fractional/horizontal/source35-margin cases. Oracle `.cache/terrain-order-probe/DungeonRouteProbe.cs`, `.cache/dungeon-route-reference.log`. Retained59 tests; wrong OverrideSteps brush length fails54/59, restored.
- Remaining source debts: LegacyDungeonLayoutProvider for Precalculate resets first room X=entranceX-10+Next(20),Y=entranceY+30 AFTER crawler's initial strength/step draws; current graph does not yet do this. Connection origin must use highest room InnerBounds.Center.X/Top, not current outer bounds midpoint/Top+4; room inner bounds need retaining. Dome/Tower building geometry and platform decoration flags/chances remain incomplete. No all-Dungeon/E9 promotion.

## Ordinary Legacy surface entrance and Old Man position — 2026-09-09

- Official LegacyDungeonEntrance.GenerateEntrance uses component-seeded UnifiedRandom for both connected buildings, strengths X25..29/Y20..24/X2 35..49/Y2 10..14. Preserve promoted .6000000238418579/.550000011920929 ratios, differing turret/battlement draw orders, support fill cutoff at double worldSurface, same-style walls/brick preservation and metadata. Initial120x120 clearing removes liquid, lava-bit and shape only; lava(false) changes kind with &2. The outer vestibule wall clearing uses double .6 and source WIDTH clamp on both axes; admitted ordinary origins keep Y safe.
- Ordinary support checks (x,y-5) never overlap the potential dungeon bounds starting at surface+10; special buried/dual/drunk/getGood entrances are not admitted by this kernel. Passage is15x3, opens toward world center; source style13 door uses three shared-RNG frame draws, inactive anchor reset and generation-only framing. No new live mutation path. PlaceWall for walls3/7/8/9 shares GenerationWallPlacement1458 with existing Corruption: center wall-frame RNG and clearing paint/coatings on empty neighbors, not cosmetic frame coordinates.
- 108 independent actual-binary GenerateEntrance fixtures (six inputs x three palettes x three seeds x two sides), CurrentDungeon=0 including NPC.NewNPC, match all16-byte fields, next shared RNG seeded6789, OldManSpawn, exclusive source component bounds (converted to inclusive graph bounds) and optional platform. Official SHA4B87890AC53D40F61DB5F928693A379ACF4CCBD8ED3B47EB32FB096F145DF034. Retained113 tests include cancellation/palette/bounds/unknown-kind guards and platform candidate order/dedup; old rectangle negative111/112 before adding unknown-kind test.
- NPC.NewNPC sets Bottom. NPC.SetDefaults(37) width18,height40; WorldFile.SaveNPCs stores position. Therefore Old Man persistence is (dungeonX*16+8-9,dungeonY*16-40), homes unchanged. Full-world regression with old record fails X56312 vs56303, then passes corrected source conversion. Existing workspace NPC persistence owner is retained; no special live NPC path. The separate Guide pass still has open floor/position/name/RNG audit; no all-NPC parity claim.
- Full Dungeon remains C in the actual audit table (some older memory prose incorrectly called it P). Dome/Tower buildings, precalculated joins, layout/feature parity and complete prefix RNG proof remain open. Counts40P+33C and31E9/279 unchanged.

## Gems and surface cleanup — 2026-09-09

- WorldGen.AddPasses ordinary Gems: type63..68 ascending, width factors .3/.1/.25/.45/.5/.05 then separate multiply .2; integer index compared with double count. Three active-stone sampling attempts, x[0,width),y[(int)surface,height), shared TileRunner strengthNext2..6/stepsNext3..7. Main.tileStone[63..68] denies active non-stone replacement; SaveSlopes true. Ore set does not contain gems, so396/397 cannot be replaced. Main.tileSand is53/112/116/234. Two sand-edge scans use opposite directions, skip only desert.Left<x<Right, InWorld margin10, and move only activity/type while preserving both cells' metadata/liquids.
- GravitatingSandCleanup is bottom-up gap filling below a falling-type cell STRICTLY above(int)worldSurface. All eleven Falling IDs admitted, including coin piles330..333 and ShellPile495. SolidOrSloped excludes actuated/tileSolidTop and inherits cracked481..483false from Dungeon and484false from Gems. ResetToType preserves wall identity but resets headers/paint/frames/liquid/shape. NOT live falling-block physics; no RNG.
- DirtWallCleanup mutates walls only, two asymmetric scans: forward ownwall2/40/64/86, reverse2/40/64; forward sand53/112/234 keeps scan open, reverse only53. Neighbor offsets-1,-2,-3,+1,+2,+3, wall2/40 only, conditional Next2 for distances2/3. Reopen checks five vertical wall cells, four vertical activity cells, sidewall±1/±2, and applies only on next row.54 actual-official full-field/next-RNG fixtures retained; old implementation fails51/58 tests. Whole-prefix/global map parity not inferred.

## Entrance and shovel — 2026-09-09

- LegacyEntranceDungeonHall ordinary slice:90 direct official full-cell/cursor/surface/shared-RNG fixtures, retained98 tests. Damping .9800000190734863 matters only after crossing worldSurface; initial81 fixtures missed its removal, newfixture9 detects6 failures. Shared TileRunner(-1) uses25..34 strength,10..19 steps,velocity0,-1; component RNG selects parameters but WorldGen.genRand aliases Main.rand for the runner. No wholeDungeon graph/feature parity inferred.
- Item.SetDefaults case4711 has no pick assignment (0), useTime14,useAnimation22. Player.ItemCheck_UseMiningTools explicitly selects UseShovel;3x3 DamageTileWithShovel invokesPickTile30, grass/jungle grass/70/633 firstPickTile100. Exact TileID.Sets.CanBeDugByShovel30 IDs retained in catalog and independent literal-set test. Existing failed-pick transformation/mining minimum/drop owners reused; shovel never enters universal pick catalog. Runtime bounded18-event burst accounts for two events per nine cells, shared with ordinary8 counter (policy, not source rate-limit). Oldtoolnegative1, oldbudgetnegative2 failures, restored.
- Falling blocks source inspected, NOT implemented: TileID.Sets.Falling is53,234,112,116,224,123,330..333,495; WorldGen.SpawnFallingBlockProjectile is server-owned, checks BlockBelowMakesSandFall and AllowsSandfall above, ClearTile then dedicated projectile spawn (center+2Y,velocityY.5). AI_010 gravity.41 cap10 with owner/server ai0 transition. Projectile.Kill restores corresponding tile or drops item and handles rails/conveyors/slopes. Need bounded wake discovery, transactional spawn/removal and same projectile/terrain authorities; never substitute falling world items or teleport blocks down columns.

- WorldGen.SolidTile and SolidOrSlopedTile(includePlatforms:false) exclude Main.tileSolidTop, not just inactive/sloped/actuated cells. Smooth World must use that exclusion: without it a dungeon platform can be copied as an unframed terrain gap fill. Existing collision catalog supplies the flag; seven regression identities cover ordinary/team platforms. This fixes a demonstrated malformed platform at3654,866 on seed3679097605675226385; no mining/liquid authority was relaxed.
- Loading KillTile(platform) -> SquareTileFrame -> CheckOnTable1x1 removes the book immediately above after its table anchor disappears. Official binary fixtures cover7 platform types x6 ordinary/Water Bolt book frames0..90; both cells become type0/inactive/frame-1, preserve wall/wire/liquid and clear block paint. Loading-only region resolution now admits exactly that bounded pair with an empty cell above the book; unknown frames, chests and further dependencies remain fail-closed. Runtime/live drops are unchanged. Current acceptance pending in work-state.
- Ordinary LegacyDungeonHall: source direction probes/retries and post-selection border/rock-layer overrides precede component-local motion; zigzag draw order and float horizontal slope/widening are retained. A depth stop still paints its final brush and advances the cursor. Shell interior resets all fields, perimeter retains fields, ClearTile preserves stored type/wall/paint/wires/liquid. Exact Main.tileDungeon six-type set is reused from the existing DungeonBrick catalog profile.135 official actual-renderer fixtures match full cells, endpoints and directions across3 palettes. These are component proofs, not whole Dungeon pass equality.

- Ordinary LegacyDungeonRoom1.4.5.8: initial and delta velocities multiply as float before promotion to Vector2D; room center is not clamped to graph horizontal bounds. Brush endpoints clamp to0..worldDimension-1 then iterate upper-exclusive. Entire outer brush clears liquid amount but not kind. Existing Main.wallDungeon cells and wall350 block shell brick replacement; inner shell Wall mutation excludes350, but cavity ChangeWallType(resetTile:true) unconditionally clears all cell fields even for350. New shell interior uses ClearEverything, perimeter preserves fields except active/type/slope. Existing VanillaWallDefinitionCatalog exactly represents Main's9 dungeon walls.63 official complete-room kernels (7 fixtures x3 palettes x3 seeds) match all normalized cells; whole Dungeon pass not yet verified.

- Ordinary Slush1.4.5.8 scans retained Ice Biome snowTop..<snowBottom and each row's left..<right bounds. Stored Silt123 becomes224 and Stone1 becomes161 regardless of center activity. Mud59 becomes224 only when no active60/70/71/72 occurs in the inclusive radius3 neighborhood. No RNG, framing, paint, wall, liquid or other field mutation. Three official whole-pass fixtures and nine real ordinary prefixes match; active-center negative control fails four retained tests. This does not establish special-seed or full-world parity.

- Ordinary WorldGen.Lakes/SonOfLakinater1.4.5.8: surface selection excludes retained Tunnels xs[5] and mountain-cave columns; source count/attempt/spacing and biome gates precede gradual biased basin carving, water-retention checks, and known wall-to-terrain fill. Twelve official basin fixtures and six complete Lakes delegate fixtures match all cells/nextRNG and lake columns. Mud59->Grass60 SpreadGrass ignores the evil-only above27 gate; existing depth-limited generation grass owner now shared by lake and evil callers. No full-world prefix or stage promotion follows from these kernel fixtures alone.

- Ordinary WorldGen.ChasmRunner/ChasmRunnerSideways1.4.5.8: main cavity samples distance RNG before CanEvilReplace, sideways reverses that order; shell passes sample independently. Main terminal altar flag never becomes true, so search repeats after the orb. Sideways wall3 uses PlaceWall, center Next(0,3), and empty-neighbor wall paint/coating cleanup; main shell writes wall3 directly. Shared AddShadowOrb changes only activity/type/frames. Source region cleanup visits every active31 cell (not object anchors), without CanEvilReplace. Official36 complete cave fixtures and6 complete Corruption pass synthetic-input fixtures match full normalized cells/nextRNG; not whole-world prefix parity.

## Evil surface grass — 2026-09-08

- Ordinary generation SpreadGrass(0,23/199) rejects x<380/x>=width-380 and strict-left/inclusive-right central45..55percent interval, but does not impose the ordinary Dirt->Grass surface-depth gate. Mud59->661/662 has no beach/spawn restriction. Above stored27 rejects even inactive. Enclosure scans x-major3x3, lava resets enclosed and breaks ONLY inner y loop. Dirt/Mud CanBeClearedDuringGeneration are true. SquareTileFrame precedes default block paint/coating reset; walls/wires/liquid retained. grassSpread limits depth1000, not totalcells (fixture Mud surface changes1591). Twelve independent official full16-byte/nextRNG goldens verified.
- Official probe must initialize empty NetMessage.buffer and Netplay.Clients arrays: otherwise SendTileSquare throws inside SpreadGrass's swallowed catch, truncating recursion after one converted tile. First uninitialized goldens invalid and NOT retained as tests. Correct fixture uses dedicated-server netMode2/no peers.

## Evil altar searches — 2026-09-08

- Ordinary late DemonAndCrimsonAltars count truncates width*height*3.3E-06; each requested altar has10000 attempts. X excludes strict45..55 percent spawn band; Y truncation precedes integer division by3. Ocean exclusion uses y <= (surface+rock)/2+40 and strict x<380/x>width-380. Shimmer exclusion distance<150. No floor scan in this late pass. Success checks stored type26, not activity. Nine official full-cell/nextRNG kernel fixtures match.
- Crimson-region search requests Next(10,15) altars, expands after101 attempts AFTER sampling, uses integer expansion/2 in upper Y, descends through air or ascends only above surface, requires active203 support until expansion>10, stops after expansion>100. Active26 within inclusive radius3 blocks placement; before deferred CrimPlaceHearts.
- Place3x2(26) rejects Boulders138/484/664/665/711..716 (665 is NOT part of QuickWater solidity override). Coordinate SolidTile2 accepts top slopes on platform identities19/427/435..439, rejects half bricks and actuation. Placement writes only activity/type/frame, preserves liquid/wall/wires/colors/shape. All18096 ordinary material/shape/actuation/style combinations match official binary.

## Crimson caves — 2026-09-08

- Ordinary CrimStart -> CrimVein/CrimEnt uses203/83; source selects5..8 veins and records at most100 heart endpoints. Direction retry consumes an initial unused pair and compares TRUNK velocity to prior vein directions; do not silently correct that source asymmetry. CrimEnt continues until20 successive steps remove no Crimstone.
- CrimPlaceHearts runs shell creation for ALL endpoints, then cavity creation for ALL endpoints, then AddShadowOrb style36/54 X and0/18 Y. Heart chamber mutations do not call CanEvilReplace, unlike cave brushes. AddShadowOrb rejects an already-active31 in its2x2 footprint and preserves remaining cell fields. Nine independent official kernels retain full-cell/nextRNG/ordered endpoint equality, including protected columns and two caves sharing deferred heart placement. Whole evil-pass parity not established.

## Evil-biome placement / protection — 2026-09-08

- CrimStart shares CanEvilReplace protection, but does NOT inherit ChasmRunner's31/22/204 excavation exclusions. Current excavation admission distinguishes the branches; CrimStart geometry and wall writes remain separate unported work.

- WorldGen.AddPasses CorruptionAndCrimson ordinary placement scans active60 jungle and147/161 snow/ice at y<worldSurface, expands actual bounds10. Draw center in[500,width-500), independent left/right Next(200)+100, beach clamp380, center margin50. Dungeon interval remains hard; central and desert guards test three points with strict inequalities. Overlapping snow/jungle exclusions shrink per attempted selection, reset for the next biome. Crimson alone applies its asymmetric dungeon-side left-edge400 correction. Current source-contract tests cover these branches; no official complete evil-pass equality yet.
- WorldGen.CanEvilReplace ignores inactive material; active tileDungeon41/43/44/677/678/679 and CrackedBricks481/482/483 reject; wallDungeon7/8/9/94..99 rejects regardless of activity. Direct official binary probe matches all553436 active/material/wall combinations. Unknown catalog identities reject in runtime generation. ChasmRunner excavation preserves31/22/204 and otherwise only calls active(false), retaining other cell bytes. This does not prove geometry, object placement or surface conversion parity.

## QuickWater contacts — 2026-09-08

- FINAL component/prefix proof: all9 ordinary Underworld comparisons match full16-byte cells, next RNG and ordered dresser slots/XY/empty40-slot contents/name. EarlyTerrainReference1458Tests retains28 stages/case (252 checkpoints), final prefix/contact44/44. Underworld promoted C->E9, bounded ordinary profile only. Remaining Lower-cut web51 kernel behavior and secret-seed branches are not claimed by this prefix proof; no general QuickWater/full-world parity claim. Negative controls6/6 old contact and12/24 reversed desert mode gate; all restored. Full sequential4728/4728, WindowsNativeAOT+6smokes/fresh dual-load pass; default-parallel allocation instability remains separately tracked.

- Full-prefix cause: LavaCheck1458 when generatingWorld and neither Remix nor dualDungeons first uses UndergroundDesertCheck radius3 (wall187 or216, InWorld defaultmargin0). On a match it sets lava(true) across the entire7x7 square, including zero-volume and active tiles, BEFORE LiquidCheck/SolidTile. No amount/material/RNG change. Existing ordinary pre-Dungeon dispatcher now includes it; loading excludes it. Twenty-four official cave snapshots verify walls187/216 and negative186 plus loading/generation distinction; reversing mode gate fails12/24. Small/Medium completeUnderworld cells/frames/RNG now all match; Large and dresser metadata still tracked in work-state.
- Correction to prior hypothesis: current ClearLiquidCell already preserves kind; live ClearLiquidCellRaw is separate. LiquidCheck sub24 lower branch explicitly resets kind to Water; shared generating/loading branch now does so.
- AttemptToMove lava/honey/shimmer uses original flags, even after fall conversion or previous contact emptied the selected cell. LiquidCheck does NOT reject changed kind/zero volume; its SolidTile guard still applies with temporary QuickWater solidity and full shape. Existing dispatcher corrected, no live authority change.
- Six official full16-byte cave100x400 snapshots (four liquid kinds, waterLine180, seeds42/1458/8675309, air-only/embedded) match. Retained QuickWaterContactReference1458Tests; old behavior fails6/6, affected278/278. FullUnderworld liquid differences remain335/2342/1468; matching water/lava lines rule out the simple threshold-input hypothesis.

## Underworld fresh-root and fort framing — 2026-09-08

- Official1.4.5.8 GrowTreeWithSettings ends with RangeFrame -> CheckTreeWithSettings. Initial left-only/right-only base frames0/66 remain correct; final base frames are66/0. Generic tree-ground root admission precedes strict633 AshTreeGroundTest. Rejected roots consume ten pairs Next(10)/Next(12) in KillTile dust selection, even during generation. CheckTree continues after removal with captured neighbours: without tree above it leaves inactive frame154,65 (left) or154,-1 (right); with tree above it retains-1,-1. Current fresh-root slice preserves this; no general tree destruction claim.
- HellFort boundary writes material/activity/shape without resetting incoming frame bytes. Flat19 platforms are framed after placement; endpoints distinguish air/platform/solid neighbours. Existing generation owner now implements this bounded slice, rejecting shaped/actuated platform interactions rather than approximating them. Independent official kernel capture:54 ash trees (pure633 and mixed2 support),12 forts (75/76, initialframes0/-1, seeds42/1458/8675309); full16-byte normalized cells and nextRNG all equal. Retained AshTreeReference1458Tests/HellFortReference1458Tests. Removing the three corrections fails49/66; restored focused324/324.
- Small full28th-prefix frame differences now zero for1458/42/8675309, RNG equal466048246/1391758357/912559166; liquid differences335/2342/1340 persist. Underworld remains C. Source kernel uses pinned official Linux binary SHA2564B87890AC53D40F61DB5F928693A379ACF4CCBD8ED3B47EB32FB096F145DF034; reflection stays in ignored diagnostic harness only.
- NEXT liquid audit, not implemented: LiquidCheck clears foreign-neighbour amount WITHOUT resetting its kind; only the sub24 lower branch explicitly resets source kind. Current shared generating/loading reaction clears via ClearLiquidCell, which resets kind. Also verify initial SolidTile guard, lower tileCut/KillTile admission and CreateLiquidMergeTile -> LiquidOverwriteStrip generation semantics before any shared-world change. Mixed four-kind stone-only kernel already differs, so webs alone cannot explain the issue. Actual full-prefix causality still needs differential proof; do not claim these observations fix all remaining335/2342/1340 cells.

## Ordinary sky/deposit/web prefix — 2026-09-08

- Final acceptance for this production revision:4638/4638 fullRelease, build0/0, sixWindowsNativeAOT smokes, freshSmall1458 dual server load and unchanged WorldCompare budgets pass; candidate00d19151... has tileL1 .201211/wallL1 .530400, not equal to reference. Exact paths/fullmetrics in work-state and paired pass audit.
- Next Underworld diagnostic (NOT implemented): official GrowTreeWithSettings ends with RangeFrame; CheckTreeWithSettings uses left-only66/right-only0 for final base frames, opposite to the growth method's initial writes. Source ground predicate for634 is exactly633. Eighteen independent ordinary ash-tree seeds show9 one-cell frame differences from current AshTreeGrower, with identical nextRNG; current scripted root tests assert only the growth writes and must be extended to final source behavior, not blindly accepted as proof. Complete Small Underworld comparisons expose additional75/76/19 frames and liquid-state differences, so existing component tests do not justify E9. No correction to shared settings initial-frame catalog is warranted merely by this observation.

- Official TileFrame dispatches485 to CheckSuper, not Check2x2. Four generated horizontal styles have a2x2 footprint, frameX=style*36+column*18/frameY=row*18. A replaced/missing cell invalidates the footprint; generation removes matching active485 remnants but preserves foreign replacements, wall/liquid/wire, and suppresses NPC582 creation. Independent official generation-mode probes cover all4 styles with each cell replaced/coherent control. GenerationDesertObjectFraming1458 now admits this incomplete-footprint slice alongside484; support-only CheckSuper remains outside it. Loading liquid rejection of incoherent/foreign objects is intentionally unchanged. The canonical Large1458 trace exposed this downstream omission after exact DirtToMud changed3158,1251 to59.

- Official AddPasses DirtToMud: ceil(area*.001) runners59, x0..width, yGenVars.rockLayerLow..height, strengthNext2..6, stepsNext2..40, ignoreTileType53 BEFORE brush RNG. Mud tail clampsvy +/-0.5, sets1 above Main.rockLayer+100, then-1 below height-300. This differs from fixed ShroomPatch-root noYChange. Source initial velocity draws and unused Next4 retained. All admitted mineral types have SaveSlopestrue/tileStonefalse; Ore overrides396/397. Active frame-important noncut cells skip before RNG; inactive identity still changes without activation, frames/wall/colors persist.
- Silt truncates FLOAT populations area*.0001f and*.0005f; both sample rockLayerHigh..height. Wall187/216 rejects BEFORE strength/steps. Ranges5..12/15..50 then2..5/2..5. Shinies has12 ordered ordinary bands with double/truncated counts, uses Reset-selected copper/iron/silver/gold and actual GenVars low/high layers; sky Silver/Gold bands and final evil22/204 remain distinct. Earlier deep-jungle recoloring/round blobs/wrong layer bands were replaced, not retained as fallback.
- Webs truncates area*.0006, consumes x/y draws even when substituting retained mCaveX/Y, scans upward then horizontal to an anchor and uses TileRunner51 with addTiletrue, speed(+/-1,-1), noYChangefalse, overRidefalse. That override can replace active non-frame-important terrain;51 clears slopes, liquid amount/lava bit, preserves other metadata. General overrides remain unadmitted. Early MountCaves now exports originalXY (not post-carve surface); independent nine-case metadata equality verified. FloatingIslands exports numIslandHouses orderedXY/style0/skyLake; existing CloudIsland/CloudLake geometry is unchanged. IslandHouse/furniture/loot remains open.
- Same pinned official Linux1.4.5.8 executable: all27-stage prefixes x9 canonical cases match full cells/RNG/pyramid candidates,243 retained checkpoints; every stage from FullDesert also has full normalized16-byte ABI. New sky/mountain side tables match.26 isolated120x600 mixed-material/shape/paint/wire/liquid kernels match full16bytes/nextRNG for13 types x2 seeds. Evidence .cache/deposits-web-{kernel,prefix-small,prefix-medium,prefix-large}.log. These are ordinary bounded proofs, not whole-world/special-seed parity. Final negative/full/AOT/current-world gates recorded in work-state.

## PlayerBot action migration — 2026-09-08

Final current-source Release acceptance:4585/4585, errors/skips0,333.828s; affected172/172, build0 warnings/errors. Negative controls confirm execution/lease cleanup, replacement-player generation refusal, monotonic observation revisions and exact addressed pickup handles. This verifies orchestration regression coverage, not an independent official-client playthrough. Native/platform gates are recorded in work-state.

This is an orchestration refactor, not new Terraria semantics. Existing weapon/ammo/combat, potion, movement, tile-assistance and Magic Mirror code remains on its prior authorities. Local official Item.SetDefaults3 case2776 rechecked: Vortex pick225/useTime6/useAnimation12; existing assistance still obeys the same underground/recent-human/natural-tile gates. Generic action deadlines, return distance and resource lease TTLs are explicitly TerraRuntime policy, not vanilla constants. Mining mode does not admit arbitrary ores/chests/structures; missing authority support remains UnsupportedAction/PermissionDenied. No new content IDs, wire shapes, game source copies or LLM integration. New lifecycle/mode validation and unchanged packet regressions tracked in work-state; full vanilla parity not claimed.

## Mining / compact item removal / pickup capacity — 2026-09-08

- Final local acceptance: Release4540/4540, WindowsNativeAOT6smokes and actual two-client TCP placement/break/drop/reservation/compact151 removal pass. Updated live_dirt_kill_probe tests the real151 flow; original user log/capture and GUI replay are unavailable, so exact incident chronology and full inventory/item-physics parity remain unclaimed. See work-state for shell-stderr diagnostics and stopped fixture server.

- Important correction to older notes: Player.PickupItem -> WorldItem.SyncItem calls NetMessage.SendData(21), but SendData's leading switch changes an IsAir item to151 before encoding. NetMessage case151 writes only Int16 slot and resets reservation255; MessageBuffer151 permits only the current server reservation owner and non-leased slot. Therefore a test sending only empty21 does NOT reproduce ordinary full pickup. Ignored independent .cache/mining-item-probe invokes actual official SendData21, yielding bytes0500971100 for slot17. Initial harness lacked peer MessageBuffers and failed after serialization; corrected initialization calls the complete method successfully. Official binary is the same pinned Linux1.4.5.8 assembly, no game source/reflection dependency in production.
- Runtime previously ignored inbound151, leaving client-picked items active until its400-slot pool filled; transactional tile breaks then correctly refused absent drop capacity. Fixed at existing frame/ingress/remove owners; slot/item/player generations and reservation ownership remain checked, leases cannot be freed by client151. Ordinary removal encoder now emits151; empty21 ingress remains accepted because source explicitly handles it, not a test fallback. No network limits raised: a single Player.GrabItems update can empty400 slots, so applying the existing240/s packet21 cap to151 would be a false kick; the existing4096/s aggregate ceiling remains enforced.
- Item.ItemSpace/CanItemSlotAcceptPickup uses slots0..49 (coins separately), then ammo handling, then occupied54..57. Item.CanStack requires equal type AND prefix; stack<slot.maxStack. Favorited OnlyNeedOneInInventory refuses, but consumable placement items do not have that restriction. Mouse58 and coin-slot partial stacks for ordinary items do not count. Source uniqueStack defaultsfalse and no assignmenttrue exists in this decompile. New reservation query admits known maxima and known favorite placement cases; unknown maximum/favorite special effects, empty-ammo routing, magnets, EncumberingStone/other CanPullItem modifiers, Void Bag and full GetItem remain incomplete. Nine direct official ItemSpace fixtures retained in MiningInventoryRegressionTests, including full/partial/favorite/prefix/cursor/coin/occupied-arrow cases. This is eligibility, not server mutation of client GetItem.
- Official Item.SetDefaults3 for2781/2786 and SetDefaults4 for3466 inherit2776: pick225. These are Nebula/SolarFlare/Stardust Pickaxe, NOT drill-only projectile types. Independent default probe finds44 pick types; all44 now match candidate. Eight unrelated item defaults require additional harness state (269/270/271/5104/5105/5106/5136/5305); no claim of complete item-default coverage. Actual literal packet5(before6) -> packet13 including selected58/prefix changes -> packet17 -> packet22 -> packet151 tested900 cycles each with full/empty inventories. Negative old151/empty-only/catalog behavior fails20/47 tests; all temporary changes restored.


## Ordinary Marble/Granite prefix — 2026-09-08

- Final batch gates: restored full4515/4515, affected221/221, Release0 warnings/errors, WindowsNativeAOT6/6 and freshSmall1458 load in TerraRuntime/official1.4.5.8 pass. Same-reference budgets unchanged, tileL1 .275338 /wallL1 .532758; whole-world still differs. Owned fixture servers stopped. Subsequent pending-gate wording below describes earlier WIP, not the final result. LinuxNativeAOT/clientplaythrough remain unverified.

- Both existing Mid passes now match the official22-stage prefix for3 seeds x3 canonical sizes:198 independent full-cell/RNG/pyramid checkpoints, full16-byte ABI and ordered padded stone-biome structure rectangles. Nine Marble and15 Granite independent kernels supplement prefix proof; ordinary full-world/special-seed/general StructureMap consumers remain unproven. Component86/86 passed. Omitted Marble slopes plus Granite lava logic fail12/40; omitting actual Granite MidPass fails all9 prefix cases. Correct source restored; batch affected/full/AOT/fresh-world gates pending.
- GraniteBiome first selects all origins, then mutates; CanPlace rejects active origins and BiomeTileCheck areas, while Place only retests origin activity. Count4..8 scales by width (not Marble's area), sampling fraction is float, retry counter is cumulative rather than reset after successful selection. Placement consumes the retained CurrentRockLayer/GenVars.rockLayer and lavaLine, never substitutes gameplay Main.rockLayer.
- Pressure uses two200x200 maps and300 iterations; resistance is4 for flat solid,1 otherwise. Bounds expand during iteration; target retains alternate-step state and must not be cleared per iteration. Center pressure25 is injected for first2 steps. Material/ore preservation, sinusoidal threshold, resistance-based clearing, snapshot-isolated thin-solid cleanup, and coordinate FastRandom65440 decorations are ordered. ClearTile preserves identity/frames/paint/liquid while clearing active/inactive/shape; ResetToType clears headers/liquid/frames but preserves ore identity.
- Granite lava conversion requires originY>lavaLine-30 and no active147/161/162/163/200 in its100x100 scan. Decoration uses unchecked stalactite placement (no PlaceTight shimmer/style draw path), local FastRandom draws and separate shared wall-framing RNG. Source-generated ice prohibition and exact lava threshold have independent full-cell kernel goldens. Shared GenerationFieldRandom1458 is the former DesertHive FieldRandom unchanged, with no legacy facade.

## Ordinary Marble — 2026-09-08, component verified / integration WIP

- Official MarbleBiome.Place builds a56x26 slab buffer, scale3, with dimensions Next(80,150)/3 and Next(40,60)/3. IsGroupSolid compares against scale/4*3, zero for scale3; any solid/sloped sample suffices. Slab slopes precede drifting column placement; fringe top/bottom offsets persist across each slab's columns.
- ResetToType preserves ore membership, retains wall identity, clears headers/liquid/frames. Placement calls center/right/left/down/up TileFrame, SquareWallFrame, neighbour-first Tile.SmoothSlope and two conditional PlaceTight attempts. PlaceTight consumes small/style draws before ordinary support rejection; CheckStalactite may retarget style or recursively destroy unsupported parts. No item drops or live authority are introduced.
- Ordinary AddPasses config scales Marble Count4..8 by world area, truncates both bounds independently, samples stratified rectangles with central45%..55% exclusion. Vertical lower bound is GenVars.rockLayer+20 (TerrainState.CurrentRockLayer), not Main.rockLayer. Structure padding8. Granite count instead scales by width; it must not reuse Marble geometry/count.
- Nine official600x400 fixtures (solid, cavern, ore/mud/paint/wire/liquid x seeds1458/42/8675309) match every normalized16-byte cell and nextRNG; MarbleBiome1458Tests retains the goldens plus rejection/cancellation/state tests. Complete-prefix/integration/full-suite/fresh-world evidence pending; no whole-world or special-seed equality claim.

## Ordinary Mushroom Patches — 2026-09-08

- Official WorldGen.AddPasses GlowingMushroomPatches selects ceil(width/700) attempts at biomes, capped50; samples central20%..80%, expands to2.5%..97.5% after width/4 failed tries while retaining the initial x draw, and stops a location search after width/2+1 attempts. The sampled200x200 region excludes active147/161/162/60/368/367 and the retained underground desert rectangle. Accepted centres must be at least500 apart; each gets one ShroomPatch plus five jittered satellites. This does not promise all nominal centres are placed (Small8675309 has five).
- WorldGen.ShroomPatch uses a vertically flattened brush, existing-wall conversion80, lava-amount clearing without kind reset, first-iteration-only lower mud activation, double horizontal movement and two roots per iteration. Its TileRunner59 arguments are strength10..<20, steps10..<20, addTile:false, speed0,2 and noYChange:true. Stone replacement consumes its source surface-jitter RNG; desert sand is excluded, mud liquid draining clears only the lava bit, and the fixed downward velocity2 is not clamped at the tail. Six official mixed-material/paint/shape/liquid fixtures independently match every normalized16-byte cell and next RNG.
- The pass calls SpreadGrass59->70 repeat:false below worldSurface with border50; it is not restricted to new patches. Lava/exposure traversal order matters. Cleanup checks the ordered3x3 neighbours, kills isolated solids, fills opposing gaps and uses the outer centre y in its grass-to-mud resets; random nearby mud-to-grass conversion also admits inactive mud. Unknown object-framing semantics are not inferred. Source KillTile generation fixtures60/70 preserve walls/liquids/wires and add no RNG; ordinary grass2 adds ten Next2 draws. Special seeds, actors occupying generation cells and complete general object framing remain outside this admitted ordinary slice.
- Independent20-stage ordinary prefixes now match all9 canonical seed/size cases by cells/RNG/pyramid candidates and retained mushroom centres. Complete16-byte ABI at Mushroom and all180 independent prefix goldens are retained. Negative controls fail14/38 (all9 actual prefixes plus5 kernels) and are restored; full4475/4475, Windows NativeAOT and fresh-world dual-server loading pass. This is bounded ordinary evidence, not whole-world or special-seed parity.

## Full Desert and world seed identity — 2026-09-08

- Integration correction: TileID484 is RollingCactus, TileID485 is AntlionLarva (earlier informal Cactus naming for485 was wrong; implementation method renamed). WorldGen ordinary SettleLiquids ends with tileSolid[484]=false; SmoothWorld inherits this, while cracked481..483 are made solid separately. WorldSmoother's static collision lookup missed484 override and both killed valid cells and cloned484 into an air gap with frames-1. The existing solidity helper now applies the exact source override. Full SmoothWorld differential/other temporary state remains open.
- Official Liquid.QuickWater on15 isolated inputs retains200 inside138/484/546/664/711..716, zero inside0/1/53/396/397; tileSolid restores true afterward. Validator1458 now reuses existing World liquid-settling fact owner including generation-only10/190/191/192;19 focused cases retain ordinary solid rejection. No budget relaxation or liquid erasure to hide invalid worlds.
- Source Check2x2 kills only same-type active fragments when any484 footprint cell is missing/replaced; under isGeneratingOrLoadingWorld it suppresses projectile727. Five official TileFrame fixtures preserve a replacement396 plus wall13/liquid100, clear remaining cells to type0/frames-1. New GenerationDesertObjectFraming1458 invokes that incomplete-footprint slice only from existing FinalCleanup; complete support/anchor framing is not claimed.
- Loading-time coherent4842x2 and4852x2 styles0..3 are now admitted in existing WaterCheck death resolution, not live mining. Twenty official KillTile fixtures touch each of four cells for all five variants; all other cells clear with type0/frames-1 while liquid100/wall13/wire survive and block paint clears. Incoherent/foreign footprints still fail closed. Independent log .cache/full-desert-boulder-liquid-official.log. New VanillaTileIds.RollingCactus/AntlionLarva match official TileID.cs.

- Ordinary source Full Desert uses DesertBiome with embedded configuration entrance chance0.5, not the class default1/3. SurfaceMap starts at50, resets the first surface when encountering cloud189/196/460/717/718/719, caps Bottom but not Average; invalid row material check reads type even when inactive. SandMound preserves source fourth-power envelope, per-column noise and wall-frame RNG. Entrances0..3 are Chambers/Anthill/LarvaHole/Pit; source shape traversal, blotch draws and vertical-tail endpoints matter.
- Cluster grouping keeps exact ordered merging/jitter and reciprocal-distance fields, thresholds3.5/1.8/.7/.25 and edge ellipse0.8. Material stream uses FastRandom with resolved WorldFileData.Seed and modifier57005, including source signed-int-to-float NextDouble behavior. Natural solidity/slope admission is bounded and unknown active materials fail closed. No live mining or generic object-framing semantics are inferred.
- Desert decoration uses source sandstone/sand conversion and conditionally ordered485/751/484/165/187 placement. Cactus CanPlace consumes Next4 even on rejection. Desert piles187 can be placed then removed by Check3x2 when any ground tile is outside53/396/397; removal resets type0/frames-1 while preserving walls/liquid/wires, unlike preflight refusal. Final cleanup performs source square wall framing plus cardinal TileFrame calls, not an extra square of tile neighbours.
- Independent official canonical prefixes now match19 stages x9 seed/size cases (171 checkpoints). Full Desert also matches complete normalized16-byte cell ABI and exported Combined-inflated10/Hive/DensityBounds/StructureArea-inflated10. Regular structure area is (Combined.X,50,Combined.Width,Combined.Bottom-20), padding10. Retaining these values does not imply all general StructureMap consumers are ported.35 isolated official geometry fixtures supplement the9 actual prefixes; source-only generation flags outside WorldTile ABI are not claimed.
- WorldFileData.TranslateSeed parses signed Int32; numeric negatives become absolute, MinValue becomes MaxValue. Otherwise ReLogic Crc32.Calculate(string) indexes the CRC table with (crc XOR UTF-16 char)&255, then shifts crc by8: this is LOW UTF-16 BYTE, not UTF-8. Fourteen official probes include Cyrillic, emoji, accented/non-byte characters and integer limits. WorldGenerationRequest.ResolveVanillaSeed1458 now owns this conversion for both pass reseeding and desert FastRandom; custom Seed remains unchanged. Restoring UTF-8 fails five golden identity tests and two actual executor tests. Omitting the production FullDesert invocation fails all9 actual prefix regressions.

## Generation Obsidian door framing — 2026-09-08

- Official WorldGen.FinalCleanup calls TileFrame for every cell. Frame-important type10 reaches CheckDoorClosed even while generating: the three active door cells and flat/unactuated solid roof/floor must exist. It does not validate every remaining row's frame alignment. Overlapping HellForts can replace a door cell before this cleanup; changing fort placement to avoid this would diverge from the source.
- Broken closed-door fragments invoke KillTile under destroyObject. In generation, Item.NewItem returns immediately without drops/RNG. Clear type to0, frames to-1, activity/inactive/shape/block paint/coating; preserve walls/liquid/wires/actuator and replacement bricks. SquareTileFrame also clears inactive neighbouring shape/block paint/coating. TileFrame has the five-cell border exclusion.
- GenerationObsidianDoorFraming1458 admits only generated Obsidian style19 (frameY1026..<1080). Unknown mixed door styles abort before removing known fragments. Existing FinalCleanup owns invocation; other door styles and full object framing remain unverified debt. Six official generation-mode TileFrame fixtures preserve wall14/liquid80, next RNG906992634 for seed1458; twelve focused tests include actual FinalCleanup invocation. Omitting that invocation fails its regression (11/12 pass), then production invocation was restored.

## Jungle and Mud Caves To Grass differential — 2026-09-08

- The ordinary prefix now extends through Jungle and Mud Caves To Grass:18 registered stages ×9 canonical cases =162 retained checkpoints in EarlyTerrainReference1458Tests. Full cells (type/wall/frame/active/liquid amount/kind), next RNG and ordered pyramid candidates match official1.4.5.8 at each stage; not a claim about all GenVars, StructureMap or special profiles. Valid logs jungle-prefix-kill-{small,medium,large} and mud-grass-after-{small,medium,large}; same pinned official binary, Framing.Initialize and all three generation flags/netMode2 are required for the reference probe.
- Jungle PlaceWall admits empty walls and the inner world only; walls15/64 consume one center reset-frame Next(3). Hole sampling uses GenVars.worldSurface+10, not worldSurfaceLow. The tunnel marks completion but executes the final iteration; its KillTile clears active terrain's type0/frames-1/shape/block paint/coating/actuation while preserving walls/liquid/wires. In generation, SquareTileFrame clears inactive-neighbour shape/block colors but skips solid cosmetic frames. Known natural types0/1/2/40/53/59/63..68/147/161 are admitted; unknown active objects fail closed. Grass2 has ten dust-choice Next(2) calls even though allocation/drop effects are suppressed. Official14-type isolated probe gives next906992634 except grass955664633 for seed1458.16 kernel tests pin these transitions and rejection.
- Jungle finishing draws gem type before position/strength/steps. Mud point rejection excludes coordinate0 and last row/column. Existing runtime budgets now throw rather than silently publish incomplete tunnels/holes or return an invented origin. These are fail-closed limits, not claims that vanilla has the same limits.
- Mud Caves To Grass calls ordinary SpreadGrass59->60 over every active cell, then ScanTileColumnAndRemoveClumps. No chance1/6, restricted rectangle or RNG. All current admitted materials are solid and generation-clearable; conversion keeps solidity/activity, making final full-grid exposure scan equivalent to the source recursive visitation.3x3 lava scan breaks its inner y loop only (later x columns can reset the flag); border10 applies. Default block colors/coatings clear; frames of active natural blocks stay unchanged in generation. Four-connected component counter visits left/right/up/down, stops at20; a run of1..19 triggers counting, components<20 deactivate only. Flood bounds include x/y5 and width/height-5, unlike the half-open grass border.12 isolated tests pin exposure/lava/borders/component metadata/cancel/unknown rejection. Source diagnostic clump counters are not retained as runtime authority.
- Full-world comparison on new native Small1458 is still unequal: tileL1 .355396, wallL1 .719732, liquidratio1.081689, silhouettecorr .926584; chests181→142, dungeon delta(+85,-6). Existing budgets pass unchanged. FullDesert, Dungeon/temple/Living Tree/Crimson, late settle and complete world parity remain open; matching this prefix does not close those rows.

## Ordinary early-prefix differential — 2026-09-08

- Official registered-pass chain Dunes through Grass now matches every type/wall/frame/active/liquid amount/kind cell, next RNG and ordered pyramid candidates for seeds1458/42/8675309 at4200x1200,6400x1800,8400x2400:16 stages ×9 cases =144 checkpoints retained in EarlyTerrainReference1458Tests. Same pinned binary as TerrainReference1458Tests; identical Reset inputs and fresh RNG per registered pass. Tile objects must be ClearEverything-reset between source cases; initialize official tile data, PostSetupContent, structures, dungeon/snow arrays and both beach-avoidance values340. Production adds no reflection/dependency.
- Dunes curve anchors use integer Point midpoints before double interpolation; overhead ClearEverything clears all data, ResetToType preserves wall ID only and resets frames0. Rocks In Dirt/Dirt In Rocks loop indexes compare against double population bounds, not truncated int. Clay40 cannot replace Sand53 at any depth. Small Holes center exclusion uses GenVars.worldSurface (Terrain current end surface), not Main.worldSurface.
- Rock Layer Caves draws type, strength and steps before x/y. Surface cave search uses y<GenVars.worldSurfaceHigh and does not create a runner when no active tile was found. Source central upper bound for the second family is1-.43. Medium1458 previously diverged by26591 cells and next RNG after Surface Caves; corrected prefix agrees. Mount Caves rejects nearby active53/151/274 and does nothing when no active surface is found before Main.worldSurface. digTunnel still rolls for brush cells outside the world before its bounds gate; Caverer's second branch sets Water255 after deactivation.
- Early Grass is NOT exposed-surface SpreadGrass: four cardinal neighbours must be active Dirt0; center becomes active Grass2 regardless of its previous type/activity, retaining frames/walls/liquids/other flags. Independent Small source fixtures previously differed by thousands of cells; all nine canonical prefix fixtures now match. Isolated production tests pin inactive/stone/dirt centers, diagonal independence, open-cardinal rejection and absent mountain surface.
- Boundaries: these are ordinary prefix cell/RNG/pyramid proofs, not all GenVars/StructureMap parity or special profiles. Dunes regular structure rectangles and Tunnels side tables remain incomplete; Jungle and later geometry/settling/content remain open. E9 evidence in the all109-stage audit is not a whole-generator completion label.

## Worldgen all-pass audit / Terrain RNG / surface-sand drainage — 2026-09-08

- `WorldGenerator.RunPass` creates a fresh `Main.rand = new UnifiedRandom(_seed)` before each enabled registered pass. Shared RNG means inside that pass, not carried from Reset. Earlier statements that Terrain observes Reset's next value are wrong: those values are Reset checkpoints only. In `Terraria.GameContent.Biomes.TerrainPass.ApplyPass`, waterLine = `(int)(Main.rockLayer + Main.maxTilesY) / 2 + Next(-100,20)` and ordinary lavaLine = waterLine + `Next(50,80)` are consumed at the END of Terrain, before the minimum-layer-gap adjustment, from its advanced RNG. Remix consumes both rolls too, then replaces lavaLine with `(int)(Main.worldSurface * 4 + currentRockLayer) / 5`.
- Candidate formerly drew these values from freshly reseeded TerrainLayers. Independent official Small probes showed seed1458 water745/candidate778, lava801/candidate846; seed42 water759/789, lava830/843; seed8675309 water895/897, lava945/976. Terrain tiles already matched. Existing Terrain now consumes/publishes both rolls; bridge copies retained state and throws if absent. No new state schema, generator or fallback. Full Terrain type/frame/active grids + liquid lines + next RNG now match on seeds1458/42/8675309 across4200x1200,6400x1800,8400x2400:9/9. Identical Reset beach inputs are supplied to both sides; this does NOT independently prove all Reset metadata or downstream worldgen. Nine official fingerprints live in TerrainReference1458Tests.
- `WorldGen.AddPasses / RemoveSurfaceWaterAboveSand` (display name Remove Water From Sand) scans x400 inclusive to width-400 exclusive, y100 inclusive to `worldSurface-1` exclusive, stopping at the first active tile. Only types53/396/397/404/407/151 (Sand/Sandstone/HardenedSand/DesertFossil/FossilOre/SandstoneBrick) trigger upward clearing of inactive cells down through y100 inclusive. It clears amount only, preserving kind bits; it does not drain embedded solid liquid, oceans or underground pockets. Evil/hallowed sands112/116/234 are not added speculatively. Candidate's former full-map embedded-sand clearing was wrong. Source re-enables LeafBlock solidity after this pass; candidate does not have that mutable global switch, and this fixture proves tile mutation only, not preceding settle-mask parity.
- Independent official registered-pass invocation on1000x600 isolated grid with worldSurface200.5, all four liquid kinds, six admitted/four rejected substrates and border/depth/cover controls matches every600000 amount/kind cell. Drained89800 units; SHA256 `852DB98DB5764633074FD8ADB10301ADFD19C0A1105E6746F732C0AE893F1632` over x-major [amount,kind]. SurfaceSandDrain1458Tests exercises the actual shipping pass with that fixture.
- Official Linux TerrariaServer1.4.5.8 SHA256 `4B87890AC53D40F61DB5F928693A379ACF4CCBD8ED3B47EB32FB096F145DF034`; ignored probes `.cache/terrain-order-probe`, terrain-order-before/after-small/medium/large logs, surface-drain-reference.log. Production does not reflect/load the Terraria assembly.
- Full AddPasses registration audit:109 source positions includes first Jungle and Skyblock conditional on skyblockWorldGen. Ordinary plan107 source positions +7 internal steps =114. Wavy Caves and Dual Dungeons Dither Snake ordinary branches are non-mutating. New complete-order test covers every applicable registration; punctuation/case normalization maps source display names to existing pass IDs. [All-stage matrix](../en/vanilla-worldgen-pass-audit.md) retains separate R9/R1/component/partial/conditional evidence; full-world1:1 remains open. Confirmed remaining approximations include FullDesert shell/chambers, Dungeon entrance shape and first/second settle sweeps/column compaction; helpers/loot/geometry of many other rows still need independent complete-pass probes.

## Plantera/Golem loot and natural prefixes — 2026-09-08

- IMPORTANT ordering correction: `ItemDropDatabase.Populate` invokes `RegisterBossTrophies` BEFORE `RegisterBosses`; `RegisterToNPCNetId` appends and `GetRulesForNPCID`/resolver retain that order. Actual official `Populate()` probe confirms Plantera starts with1370, Golem1371, Queen Slime4958, Eye1360, Destroyer1366. The new Plantera/Golem evaluators roll the trophy BEFORE bags/relics/pets/Classic rules. Prior notes or older evaluator comments saying trophy comes later are incorrect; their follow-up correction is still open. This does not imply inclusion of global rules, which precede NPC-specific rules separately.

- Official1.4.5.8 `ItemDropDatabase.RegisterBoss_Plantera`, `RegisterBoss_Golem`, `RegisterBossTrophies`, `Conditions.FirstTimeKillingPlantera`, `OneFromRulesRule` and `CommonCode` define the imported NPC-specific death slice. Plantera first-kill child precedes mask/key/other children; first kill is `!NPC.downedPlantBoss`. Repeated Classic kills select eight nested CommonDrop rules using raw Next(1), Next(8); launcher758 chains RocketI771 stack50..150. Classic always drops TempleKey1141; Expert receives bag3328 instead. Optional Plantera items:2109/7,1182/20,1305/50,1157/4,3021/10; trophy1370/10. Master relic4934 and pet4806 use existing common/per-interactor raw rule semantics.
- Golem Classic order: mask2110/7, Picksaw1294/4, MobiusStrip6158/6, raw Next(1), Next(7) nested reward1258/1122/899/1248/1295/1296/1297, then BeetleHusk2218 stack4..8. Stynger1258 chains bolts1261 stack60..180. Expert bag3329; Master relic4935/pet4807; trophy1371/10. CommonDrop keeps luck chance and raw stack calls distinct from OneFromRules raw selection. Bag contents/global rules are not part of these evaluators.
- `CommonCode.DropItemFromNPC` and instanced/per-player variants request natural Prefix(-1). `Item.SetDefaults`, `PrefixLegacy.Prefixes/ItemSets`, `Item.TryGetPrefixStatMultipliersForItem` and `Item.Prefix` were checked directly. All19 Plantera and16 Golem drop dimensions/maxStack/families were read from official server instances. Family order is source data, not a generic weapon behavior admission. VenusMagnum useAnimation9 rejects prefixes20/45; HeatRay animation10 rejects45; other imported family members pass the source stat guards.
- Independent local probe loaded official Linux TerrariaServer1.4.5.8 (SHA256 `4B87890AC53D40F61DB5F928693A379ACF4CCBD8ED3B47EB32FB096F145DF034`) and compared all35 items over seeds0..99 against the candidate, including the next raw RNG value after natural prefix resolution:3500/3500 matched. Probe/logs are ignored `.cache/plantera-probe`, `.cache/plantera-probe.log`, `.cache/golem-probe.log`. Production uses no reflection or game assembly. Scripted and actual kill/replication regressions pin source ordering and unknown/stale/clientless/duplicate rejection.
- Door continuation source facts only, NOT an implemented feature: `Player` locked-door branch scans inventory0..57 for key1141, consumes one, unlocks locally, sends52/action2; Void Bag is a separate fallback in the original game. `WorldGen.UnlockDoor` normalizes to frameY594 and adds54 to three vertical cells. Existing NPC door-opening authority does not establish player packet52 ownership or key-consumption ordering.


## Bot cave assistance and recall, 2026-09-08

- Local1.4.5.8 ItemID.VortexPickaxe=2776; Item.SetDefaults3 case2776: pick225, useTime6, useAnimation12, useStyle1, tileBoost+4. Player.GetPickaxeDamage yields225 for ordinary Stone and450 for Dirt; PickTile commits at accumulated100. Current bot policy admits only these two one-hit materials and waits12 ticks; no guessed prefix/equipment speed, ore or structure semantics. Player.DefaultTileRangeX/Y=5/3; assistance deliberately stays within64/48 pixels, narrower than base+item reach. Underground permission uses persisted surface and both actor top edges strictly below it, a conservative policy beyond vanilla's position/worldSurface layer checks.
- Player.Spawn_SetPosition recall anchor maps to x=floorX*16-2, y=floorY*16-42 for20x42 body. Validate THIS body, not pre-rounding center. Search radius12 tiles, dry-body refusal, recent mining120-tick observation,3-tile observation neighbourhood, local cave routing and squad assignments are TerraRuntime policy, not invented Terraria rules.
- Source Collision.SolidCollision clamps its scan to maxTilesY-40. Bot route/recall policy refuses bodies reaching that unexamined bottom strip instead of interpreting the absence of scanned collisions as clearance. Existing vanilla collision implementation is unchanged.
- Existing WorldTileAuthority records only a completed active simple-cell pick break after commit/drop publication. Fail1/unsupported/explosion echo/failed mutation do not become observations. Exact PlayerHandle and per-world authority prevent another player/reconnect/world from inheriting the observation. Bot break uses existing simple-drop reservation and mutation/replication; no fabricated client connection or client-issued bot mining command.

## Player flight / ordinary sky geometry, 2026-09-08

- Player.Update calls WingMovement under flag19 and ordinary gravity ONLY under !flag19. Previous shared stepper incorrectly added.4 after active wings, canceling sustained ascent. WingStatsInitializer[26]: Fishron180 ticks, speed override8, acceleration multiplier2 while airborne. Item4989/EmpressFlightBooster: moveSpeed+.075; UpdateJumpHeight adds1.8; Update multiplies runAcceleration1.75 after wing tweaks. WingMovement decrements first, then Insignia refreshes to max ONLY if remaining!=0. Normal dry Fishron acceleration-.125 with extra-.75 while falling or-.15 above -jumpSpeed, cap-jumpSpeed*2.5. Functional equipment gates authority; no inventory/vanity shortcut. Rocket fuel conversion/other wings/compound context parity remains open.
- New ordinary CloudIsland/CloudLake geometry directly matched official Linux1.4.5.8 in10 fixtures, seeds42/1458/17/1/8675309, empty600x400 candidate at300,150. Compare x-major type/wall/active/liquid/kind and next RNG; independent hashes retained in SkyIsland1458Tests. Reflection exists ONLY ignored .cache/sky-diff, no production dynamic loading. Cosmetic frames are excluded. CloudIsland interior SquareWallFrame resets ONLY center and consumes Next(0,3) per accepted wall; CloudLake does not. Missing this draw changes later water/pockets. Lake core admits y>surface-2, strict radius*.4, only189 (not196), clears six walls, fills only y>surface+1 with source three-neighbor holding predicate, vertical drift becomes0 after first step. Island core changes189 to0 without requiring active, underside restores type189 without activating. Source floating pockets jitter Next(-1,2); island/lake pocket count/rain odds differ. No invented Grass conversion or shallow blanket fill.
- FloatingIslands ordinary requests use width-1 attempts, nested strict midpoint±150 rejection, strict existing-anchor±180 rejection, ground scan from200 while<worldSurface, Next(90,ground-100) then min(worldSurfaceLow-50). Exhaustion skips the request, never invents evenly spaced anchor or surface fallback. These helpers are ordinary-only; full house/content/final-world frame/visual parity and Dungeon geometry remain open.

## Ordinary Underworld terrain and generation fluid boundary, 2026-09-08

- Fresh Small1458 executable load additionally refused tile227 at3125,951. Official TileObjectData.StyleDye width1/height1, CoordinateWidth32+Padding2; WorldGen.PlaceDye sets frameX34*style, frameY0. Writer incorrectly used18*style; loading had no single-cell227 admission. Shared VanillaDyePlantFrame1458 now covers only known styles0..11, rejects malformed frames, and loading noItem removal stays separate from live loot/mining. All12 valid and4 invalid frame tests plus3 canonical generation/.wld/load/prepare tests pass. Placement density/style-selection remains partial. Corrected Small independently reached CoreCLR17842 and official Windows1.4.5.8 port17843; both processes stopped, official exit-nosave returned0.

- Source: pinned1.4.5.8 WorldGen Underworld, TileRunner, Liquid.QuickWater/SettleWaterAt, ShimmerRemoveWater, LiquidInteractionsCleanup, WaterCheck. Initial roof is height-Next(150,190), clamped after Next(-3,4) to height-190..height-160; coating starts roof-20-Next(3), changes type even on inactive cells, carve preserves wall/frame/shape/paint. Basin is height-Next(40,70), draws Next(-10,11) in columns10..width-11; crossing above height-100 jumps to height-120, not a normal clamp. No solid lower slab or periodic puddles. Hellstone count truncates width*height*.0008.
- Small Underworld TileRunner types57/58/-2, strength below30 and steps<=1000: initial random X/Y velocity draws occur even when overridden; ordinary Next(4) is consumed. Shrinking bounds but original-strength Manhattan jitter; frame-important non-cut objects skip before RNG. SaveSlopes57/58 true; generation-clear table reused, desert396/397 override permits Ore58, protected natural types retained, -2 active Sand never carved. `addTile` activation/liquid clearing is independent of replacement permission. Cave liquid gates strictly y<waterLine or y>lavaLine. No early exit after runner leaves map: remaining velocity draws still occur. Direct official Linux binary reflection matched21 fixtures tile-by-tile and next RNG;7 seed42 golden cases committed as regression data, no game methods/assets committed.
- Underworld QuickWater(-2) scans the WHOLE map, -2 is only progress verbosity. Before Dungeon/Aether, dungeon bounds are absent and Reset shimmer position is0,0. Ordinary vertical fall below waterLine turns water into lava, but not honey/shimmer; strict equality/no vertical fall do not convert. Shared runtime flow reused. Pre/post ShimmerRemoveWater clips radius150 to y<=75 at the reset origin; type162 deactivated. Cleanup converts isolated56/659 merge blocks back to liquid, ClearEverything metadata reset, and source counts ABOVE shimmer as WATER. Official8 flow/interaction fixtures matched type/active/amount/kind (verbose0 avoids loading localization only); clearing liquid amount now preserves empty kind in the shared ClearLiquidCell boundary as in source.
- Existing later generation settling omitted WaterCheck solid clearing: the first composed-map run failed strict solid+liquid validation after source Underworld flow moved water. Both late settle owners now invoke only that verified slice with Bubble/actuation/solidTop and worldGenTilesIgnoreWater exceptions10/190/191/192 plus shared boulder/546 exceptions. Full repeated generation settling, post-Dungeon/Aether flow and object side effects remain open; the existing downward sweeps/column compaction are NOT relabeled vanilla-exact.

## Acceptance tools, 2026-09-08

Pinned Windows1.4.5.8 SHA256 d87e3faf08637f6be8882c63e7f11fb7e792b0230006309618473ece0f863e1e was decompiled with the CI ILSpy11.0.0.9375. Missing XNA (Linux has no Windows GAC) renders Moon Lord's death target Vector2 as default + by-ref .ctor temporary before Lerp; explicit no-XNA resolver locally reproduces the CI failure. Source matcher now verifies that exact adjacent constructor/local/operand/amount sequence as well as resolved output. Both complete source checks pass; negative fixtures reject swapped operands, overwritten/different temporary and changed constants. No Moon Lord AI change. Live TCP Dirt probe must change actual hotbar selection, since RuntimeConnectionRegistry suppresses identical packet13; same-slot item replacement does not force movement replication. World boundary and authoritative tool checks are unchanged.

## Rails and ocean teleport (2026-09-08)

Final narrow-body admission also rejects MovementMountPresentFlag before invoking the ordinary20x42 landing query. Do not infer the physical mounted rectangle from the item or default player body; that slice is still open. Real packet73/65 tests pin this refusal. Direct official Minecart.Initialize (dedServ=true) plus FrameTrack independently matched all14 literal connection pairs with back=-1, in addition to the12 ocean cases below.

- Official Minecart.Initialize/FrameTrack: tile314 frameX is an index, NOT atlas pixels. Ordinary connection pairs (left/right vertical offsets; absent=-2) map 0:absent/absent,1:0/0,2:absent/0,3:0/absent,4:+1/0,5:0/+1,6:0/-1,7:-1/0,8:-1/+1,9:+1/-1,10:+1/absent,11:absent/+1,12:-1/absent,13:absent/-1. BackTrack=-1 means no switch. Generated all0/0 was an actual disconnected-track defect. Straight/slope/end reciprocal frames now survive canonical final generation. Unsupported one-cell peak/valley rejects before carving, not a guessed frame.
- TrackGenerator.PlacePath initializes clearance6, redraws Next(5,9) when Next(7)==0, clears railY..railY-height+1, then ResetToType314 retains wall type but resets liquids/paint/actuation; four wire bits are restored. Current generator preflights maximum eight-cell clearance for protected objects before mutation. Full path/origin/history/tunnel/smoothing/pressure/wall finishing and cross-track RNG equality are still open, not proved by connected frames.
- Player.ItemCheck treats item4263 and ShellphoneOcean5360 identically: client packet73 subtype1. Player.MagicConch compares position.X/16 to integer maxTilesX/2; LEFT means request RIGHT with crawl-1, then original LEFT/crawl+1 only on failure. RequestMagicConchTeleportPosition starts x40 or width-40,y50; limit5000 iterations/400 horizontal steps, follows water/solid/side-clearance, extended body collision, hazards, worldSurface, and final20-cell support scan. Air-cell landing converts to x*16+8-width/2,y*16+16-height. Old authority inverted ocean choice and scanned underground cavities. Twelve direct official helper calls match both-side dirt/grass/platform/sand/spike/Hellstone results; sand also belongs to source Suffocate/CanTileHurt, so it can move the final landing farther inland. Evidence `.cache/rail-conch-diff` is local-only reflection, not production dependency/source copying.
- New World helper uses existing SolidCollision/LavaCollision/TileCollision. Requires verified surface and ordinary fixed player20x42; Skyblock lowTiles and conditional equipment/secret-seed hazard exemptions remain fail-closed. Bounded malformed-edge refusal retained. Source NetMessage65 number6==1 sets bit2; MessageBuffer65 uses receiver's current position on that bit. Encoder now carries failed=true instead of ignoring it. Literal packet73->existing composition->packet65 repeated-use and missing/Skyblock-fact rejection tests cover actual authority. Existing section streaming retries on packet13; proactive pre-teleport CheckSection ordering is NOT implemented by this correction and remains explicit debt.

## Guide doll / release ownership continuation (2026-09-07)

- Source WorldItem.FindOwner(forceAssignToServer:true) sends packet39 on the owning multiplayer client, NOT22. NetMessage39 payload is signed Int16 slot plus BinaryWriter bool. MessageBuffer39 permits only current owner, resets owner255 and calls FindOwner(force); packet22 is still ignored by dedicated ingress. Runtime adds exact-generation/playing checks and bounded3-byte decode; bool follows BinaryReader nonzero semantics. No requested owner/position crosses this packet.
- WorldItem.DefaultGrabDelay=100. ApplySpawnOwnership passes local context from MessageBuffer21 into reserve/local-grab-delay/all-grab-delay. UpdateItem decrements positive grabDelayTime/timeToKeepReservation/enemyGrabDelayTime each update. Initial allocation previously lost those facts, making a just-thrown doll immediately reserve back to its thrower. Now source modes and timers use the existing store; silent timer/motion updates are not per-tick packet21/22 broadcasts.
- Item.SetDefaults267 accessory=true,width14,height26. Only these motion/contact facts are newly admitted, not a general item-use/prefix catalog. Ordinary WorldItem.UpdateItem has gravity.1/cap7; prior wet .08/5, prior honey .05/3. Wet displacement captures incoming velocity*.5 or*.25 BEFORE new gravity/drag(.95, strict double ±.1 snap). MoveInWorld computes full LavaCollision and central WetCollision BEFORE motion; lavaWet persists while wet. TileCollision then ApplyMovement then SlopeCollision precede CheckLavaDeath. New pass admits flat unreserved267 only; slopes/platforms/conveyors421/422/shimmer and oversized motion fail closed. Broader motion, item attraction/stacking and generic rarity burns remain open.
- Previously audited whole-stack removal/Guide9999-knockback10-direction/SpawnWOF/extra town-victim order is now implemented through existing item removal and NPC non-player damage/spawn boundaries. Source isLikeATownNPC is townNPC OR453; ordinary housing-eligible types plus37/368/453 are admitted, not inferred from friendly alone. Dynamic town identity and deprecated/unknown types are not generalized. No Guide still consumes whole stack; all Guides struck even with stack1; remaining stack chooses admitted victims without replacement; existing Wall/depth rejection still happens AFTER Guide strike. Boss kill, not summon, owns Hardmode.
- Spawn placement preserves strict midpoint and ±1200 top-left active-player exclusions (dead players included), source search up-before-down offsets0..998, liquid<100, SolidTile excluding slopes/half bricks/platforms and final height-190..height-120 band. Exhausted search keeps the source starting height before clamping; target defaults255. No full announcements/Bestiary/whole encounter claim. Regression tests include real network packet21/39 ingress and runtime ticks, source literal bytes, item/connection generation rejection and flat-air descent into lava.

## Server-owned player lava continuation (2026-09-07)

- Official Player.Update checks lava before movement, after UpdateBuffs/UpdateLifeRegen. Player.UpdateEquipFunctional gives Terraspark5000 waterWalk, lavaMax+=420 and lavaRose. Collision height is42-6 for waterWalk; this differs from the later waterWalk2 wet probe. Current random bot kits do not include Terraspark: the older blanket equipment assumption below was stale. Functional equipment, not vanity or bot type, supplies protection.
- Lava cooldown is independent hurtCooldowns[Lava=4]. With no cooldown, consume positive lavaTime; otherwise Hurt(ByOther2, ordinary80/Remix200, direction0, cooldown4), then only positive surviving damage adds OnFire24 for420/630. Rose subtracts45 damage/210 duration. Dry ticks refill one; clamp to max at end. Player.Spawn/recall refills lavaTime. The runtime uses its existing armor/HP/death pipeline, not direct bot HP writes.
- Ordinary OnFire contributes negative8/120 to life regeneration, not Hurt; UpdateLifeRegen death is KillMe(ByOther8,10,0). Known Vampire worlds are excluded because source uses100 rather than8. Water/honey contact clears OnFire after that update's regeneration. Runtime owns duration and generation-bounded counters for clientless players only, publishes retained packet50 types (including late join), and sends packet118 with independently pinned literal death bytes for lava/burning. GodMode skips HP loss; death resets environmental state. Unknown equipment/world facts fail closed. This is not a general buff/natural-regeneration engine, human client environmental authority, mount/shimmer/ObsidianSkin/AshWood/compound-debuff parity or all lava accessories.
- Real default bot, explicitly boot-equipped bot, GM/death, source420 expiry, recharge/recall, vanity/unknown equipment, wet extinguishing, independent immunity and slot reuse tests pass. Removing the actual ServerRuntimeState lava invocation causes17 of48 selected tests to fail; restored full Release3996/3996. NPC buffs/shared immunity and item burning are unchanged.
- Next item's independently inspected source: WorldItem.CheckLavaDeath requires reservation owner Main.myPlayer (dedicated255);267 uses VoodooDollLavaDeath before rarity checks. Server removes the WHOLE stack first, strikes every active Guide22 with9999/knockback10/direction, calls SpawnWOF per Guide; no Guide still consumes the doll. Remaining stack can kill random town-like NPCs. NPC.SpawnWOF rejects positionY/16 < height-205, existing wof index/type113 or client mode; chooses side from world midpoint, shifts horizontally away from active players within1200, searches non-solid liquid<100 vertically with1000 budget and clamps height to UnderworldLayer+10..+80. These facts are audited, NOT implemented; do not substitute generic SpawnOnPlayer or remove only one doll.

## Tester biome/lava follow-up (2026-09-07)

- Official `NPC.Spawner.SpawnAnNPC` ordinary Underworld branch is strictly `spawnTileY > maxTilesY - 190` (not the spawn-budget height-200 threshold). Ordered short-circuit draws: eligible unsaved hardmode Tortured Soul534 Next20/Any534; lava bait Next8; Bone Serpent39 Next40/Any39; Fire Imp24 Next14; demon Next7 then Voodoo66 Next10, otherwise post-mech hardmode RedDevil156 Next5!=0 or Demon62; LavaSlime59 Next3; post-mech LavaBat151 Next5!=0 or Hellbat60. Existing actors do not skip the preceding RNG draw. Multi-actor bait is not an ordinary slime fallback. `VanillaUnderworldSpawn1458` pins these choices; NpcAuthority retains separate definition/AI admission, rejects missing facts/special or competing biome branches, and combines persisted/live progression. Fire Imp currently has defaults/partial planner but no AI coverage admission; do not paper over that discrepancy. General biome spawning still incomplete.
- Official `Collision.LavaCollision` tests the complete physical rectangle, independently of central `WetCollision`, with strict intersection, liquid-surface fraction `(256-amount)/32f*2f`, exclusive x bound width-1 and y bound height-40. New dedicated helper is used by the NPC contact pass; existing projectile central-liquid behavior was not silently rewritten.
- Official `NPC.Collision_LavaCollision`: type441 collision suppressed; non-lavaImmune/non-dontTakeDamage and immune255==0 -> immune255=30; ordinary AddBuff24/420 + StrikeNPCNoInteraction50, direction0/knockback0. Remix hostile instead gets buff24/180 and no direct strike. New runtime implements ONLY direct ordinary contact for verified default-vulnerable1/2/3/21/22, after committed motion, generation-keyed contact cooldown and shared existing non-player damage/death/loot finalizer. No player interaction is invented. OnFire/DoT/replication, shared cross-source immunity255, exact collision ordering, other NPC flags/seeds remain open. Unverified NPCs are not assumed vulnerable.
- Player lava is separate: Player.Update uses equipment-dependent lavaTime/lavaMax, lava immunity/rose/Ash Wood, dedicated hurtCooldowns[Lava], source base80 (Remix200), OnFire and ByOther2. Bot functional Terraspark5000 makes naive immediate80 damage incorrect. This was inspected, NOT implemented in this pass.
- `WorldItem.CheckLavaDeath`/`VoodooDollLavaDeath` own ordinary rarity/immunity burning and type267, with server reservation, whole-stack removal, Guide strikes, source SpawnWOF and stack-dependent additional town-NPC effects. No such runtime pass yet; don't equate deletion with completed WoF progression.
- WorldGen ordinary Underworld source begins roof random walk, continuous initial lava basin, ash runners, QuickWater(-2), further ash/cavity runners, two-row restoration, Hellstone runners, vegetation/forts. Current MidPass still substitutes continuous solid floor and x%19 puddles. Prior restoration/fort/forest tests do NOT prove terrain/visual parity. Other tester final-world geometry reports still await exact world and absent screenshots.

## Duke ocean/enrage and Underworld lava restoration (2026-09-07)

- Official NPC.AI_069_DukeFishron compares player.position.Y<800 or >worldSurface*16, or6400<player.position.X<maxTilesX*16-6400 (all strict, not Center). Enrage sets hover10, damage=defDamage*2, defense=defDefense*2, dash speed+6. Phase1 bubble selection becomes shark state3 with incoming timer50 while preserving selected cycle1; ordinary shark selection resets cycle0. Phase2 circle becomes state8 with timer0. Life-phase transitions still win. Phase3 also uses hover10/dash33, but teleport/dash duration remains unchanged. State8 SharknadoBolt385 receives ai2=1 only when enraged; existing AI65 consumes it for homing speed. Returning to ocean recomputes stats/timing; no persistent enrage flag.
- ScaleStats_ByDifficulty applies EnemyDamageMultiplier1/2/3, then type370 damage tweak.7 in Expert/Master with Math.Round, then sets defDamage. Ordinary base damage100/140/210; AI69 phase2 int(base*1.2f*expert1.2f), phase3 int(base*1.1f*expert1.2f). Runtime contact damage directly consumes DamageOverride, so catalog-only100 was incorrect in Expert/Master. Forty-three golden/source tests include exact boundaries, phases, enrage provenance and actual NpcAuthority width routing. Missing verified world bounds rejects root/projectile plan. Whole spawn life/difficulty/secret-seed parity and distant-target/despawn ordering remain open.
- Official WorldGen Underworld after carving and before Hellstone loops EVERY x and fills inactive cells at maxTilesY-145 and-144 with liquid255/lava(true). Tile.lava(true) replaces liquid-kind bits; active actuated tiles are still skipped. No RNG draws, wall/type/frame/paint changes or final-sea-level promise. Existing MidPipeline now invokes UnderworldLava1458 at that boundary. Eleven tests include all canonical heights and real Small/Medium/Large pass observation before final liquid compaction. Initial basin, QuickWater and terrain/ore runner equality are still unimplemented.

## Bot observer/combat corrections (2026-09-07)

- Official1.4.5.8 MessageBuffer case13 sets gravDir=+1 only when movement flags bit4 is set. MiscFlags2 bit6 is lastItemUseAttemptSuccess: remote Player.ItemCheck ANDs ItemCheck_TryStartUse with it. ControlUseItem bit5 alone does not start a visible swing. Server-owned snapshots now carry both correct states. Ordinary aimed useStyle5 ItemCheck_Shoot sends packet13 followed by41, whose payload is byte player + float rotation + short animation; rotation=atan2(vy*direction,vx*direction)-fullRotation. The admitted upright bot uses fullRotation0, actual launch vector and weapon animation ticks.
- AI_001's flag3=false switch includes Jester's Arrow5, Bullet14, GreenLaser20 and SilverBullet981. Default ai0 does not advance, so the generic ai0>=15 gravity branch never begins. Same-aiStyle does not imply same gravity. Runtime/predictor previously treated them as falling basic arrows; fixed with shared admitted facts. Independent wire/source assertions and120-subupdate behavior tests cover this, not just codec roundtrip.
- MessageBuffer case14 resets remote Main.player active state; packet7 world replacement does not clear that array. Cleanup excludes own wire slot and sentinel255, then destination player baselines reactivate only destination actors. Three repeated Level1 route transfers cover an existing bot; no evidence yet for the tester's exact Lost connection.
- Official TerrariaServer 1.4.5.8 `MessageBuffer.GetData` case `29` reads a raw projectile key plus final position. A resolved active projectile owned by another server client is ignored for local mutation; an unresolved key is likewise a local no-op. A finite position is assigned before `Kill`; non-finite X/Y instead sets the resolved projectile inactive. Except for the foreign-owner early exit, the dedicated server relays packet `29` to peers in either case. Therefore packet-29 transport decoding must preserve raw key and IEEE-754 position bits for relay, while the authoritative projectile registry separately requires an exact addressable generation and finite position before any local position mutation.
- Player.Hurt(pvp:true) uses8 immunity ticks; existing damage pipeline remains the calculator for bot PvP. MessageBuffer118 carries death reason, damage, hitDirection+1 and PvP bit0. Bot death now supports authoritative player reason as well as NPC reason. Unsupported remote packet55 bot debuff duration/effects remain outside this correction.

## Effective object liquid death / real startup refusal (2026-09-07)

- Official TileObjectData constructor defaults `_useGlobalLiquidChecks=false`; ApplyNaturalObjectRules opts in. GetTileData(Tile) uses full coordinate sizes, StyleHorizontal/WrapLimit/Multiplier/LineSkip, then subtile and matching alternate, not visual-wrap overrides. Main's10-water/267-lava arrays are only raw tables. Obsidian furniture and platform19/style13 are lava-proof;93/style23 is water-vulnerable/lava-proof. Lantern42/style32's inherited alternate0 is lava-vulnerable although CheckLavaDeath(type,style) reports false. FullCopyFrom82 loses subtiles because TileObjectSubTilesModule's copy loop checks the new empty list's Count; style5 immunity exists only on82, not83/84. Preserve these source quirks.
- Independent direct official Main.Initialize_TileAndNPCData1/2 + TileObjectData.Initialize/Check*(Tile) differential: all754 types with fixed51x51 frames plus metadata-derived styles0..512/alternates/corners,3,853,832 nonnegative cases, zero differences/exceptions/rejections. Official Linux EXE SHA256 `4B87890AC53D40F61DB5F928693A379ACF4CCBD8ED3B47EB32FB096F145DF034`; fixed1,961,154-case hash `12DB9C63AC0CFF9128E5891FB71F56BCDBC63C05C1297A198C23AA28C37E7909` retained in unit test. Extra negative-frame matrix has483 conservative candidate refusals, other cases agree. Probe reflection is ignored local evidence only; production is static/AOT-safe.
- WorldFile.LoadWorld sets isGeneratingOrLoadingWorld; KillTile forces noItem and Item.NewItem independently refuses creation. CheckOrb12/CheckPot28/Check1xX93/Check3x2215, Check1x2Top42/CheckBanner91, painting Check*Wall and CheckJunglePlant233 remove remaining coherent members. CheckTileBreakability checks locked-door-below for EACH KillTile, including non-solid objects. Platforms set exactly19/427/435..439. KillTile retains actuator/wires, clears active/inactive/block paint/coating/type and frames to-1; subsequent inactive TileFrame clears shape, not actuator. Full neighboring SquareTileFrame cascade parity remains open.
- Existing generator's one-cell233 was invalid, not a valid conservative representation. PlaceJunglePlant233 styleY0 requires3x2, anchoroffset(-1,-1), frames54*styleX+18*column and18*row, flat unactuated60 support in all3columns, source5-cell bounds, inherited center-support block paint/coating, independent liquid/wall/wires retained. Clear-footprint styles0..7 are now admitted; source replacement/cascade contexts and generation ofstyleY1 remain open. Loading separately admits coherent3x2/2x2 detritus and rejects malformed remnants without erasing foreign cells.


Last evidence refresh: 2026-09-07.

## Underworld painting and ceiling decorations

- Source: official1.4.5.8 WorldGen.AddHellHouses tail, RandHellPicture, nearPicture/2, PlaceTile -> Place3x3Wall/6x4Wall/2x3Wall/3x2Wall/Banner/Chand/1x2Top and corresponding Check methods. Each phase ceil(420000d/width) requests100/66/50. Painting initial house-air sample plus100001 retries; ceiling do/while up to100001 samples; over-budget final samples are discarded even if valid. Ordinary x centralhalf,y[height-250,height-20), wall13/14 inactive.
- Painting repeats horizontal house-air midpoint then vertical midpoint twice. Final horizontal/vertical three-cell empty strips ignore walls and share the pre-recentered point. Accept span>7/>5. nearPicture2 ordinary inclusive x±8,y±5 considers only active240/241/242; dungeon walls7/8/9 expand to±15/±10. Small245/246 deliberately excluded. RandHellPicture draws Next4, redraws family1 once; family0->240 styles27/29/30/31/32 viaNext5;1->242/style14;2->245/styles1/2/4;3->246/styles0/16/17. This occurs BEFORE nearPicture's any-active asymmetric x[-4,+3],y[-3,+2] gate.
- Full nonzero-wall inactive footprints:2403x3 offset(-1,-1),frameX54*style;2426x4 offset(-2,-2),style14 frameY1008;2452x3 offset(0,-1),frameX36*style;2463x2 offset(-1,0),frameY36*style. All18pixel steps. PlaceTile cleans inactive anchor only; liquid/non-anchor paint preserved.
- Ceiling palette makes all3 Next(16,22) initial draws before duplicate retries of index1 then2. Upward SolidTile scan excludes platforms; ceiling candidate wall13/14 required. Next3 chooses banner91/style frompaletteNext3, chandelier34/style32 (3x3,offsetx-1,frameY1728), lantern42/style32 (1x2,frameY1152). Banner1x3 frameX18*style. Source apparent clearance loops repeatedly read only anchor[x,y], so no invented neighbor exclusion; full placement footprint and central support remain checked. Production bounded to ordinary generation and verified styles; malformed vertical openings fail closed, palette search exhaustion aborts rather than fabricates style.
- Focused56 plus24 full-worldgen tests passed; canonical Small/Medium/Large seed42 check complete retained decoration footprints. Negative control omitting real invocation fails all3 with zero paintings/banners/chandeliers/lanterns; restored. This closes ordinary settlement decoration generation only, not Underworld terrain/ore/lava equality, special seeds or universal runtime object semantics.

## Underworld ground furniture and dresser persistence

- Source: AddHellHouses after torch phase, PlaceTile, Place3x2/4x2/3x4/2xX/2x1/1x2/1xX/2x2/OnTable1x1, SolidTile/2, Chest.FindEmptyChest/CreateChest/CreateWorldChest, official1.4.5.8. Budget is ceil(4200000d/width), i.e.1000/657/500, not area-scaled. Initial x[quarter,width-quarter), y[height-250,height-20) plus up to100001 retries seeks inactive wall13/14; exhausted source request skips. Scan down uses SolidTile (full platforms excluded), then center of contiguous empty-on-solid floor. Source span is right-left, not cell count.
- Choice0..12 clearance half-widths[5,4,3,4,3,5,5,5,5,3,5,2,3], heights-above-floor[4,3,5,6,3,3,4,4,4,5,3,4,3]; inclusive empty rectangle and span>=halfWidth*1.75 gate before secondary RNG. Choices: table14/style13 plus candle33/style25 via Next6 offset<3 and chairs15/style16 at±2; workbench18/style14 plus candle Next4 offset<2 and one random side chair; statue105/style49; bookcase101/style4; random-facing chair; bed79/style8; piano87/style15; dresser88/style9; bench89/style10; clock104/style17; sofa90/style25; lamp93/style23; candelabra100/style25.
- Chair source frameY period40, right-facing adds18 to both frameX. Bed/sofa direct Place4x2 bypasses anchor cleanup, direction+1 frameXbase72, style frameY36*style. Other objects enter PlaceTile cleanup of inactive anchor only. Lamp checks all3cells dry; normal furniture preserves liquids. Candles use own table/workbench support. Runtime/general placement and other support contexts are not claimed by this scoped cleared-room helper.
- Dresser Place3x2 writes six18-pixel frames and calls Chest.CreateChest at x-1,y-1. FindEmptyChest rejects exact duplicate coordinates/capacity; ordinary new storage is40 empty slots, no loot roll. Existing Workspace registry already supports tile88 via object catalog but BOTH worldgen validators wrongly hard-coded21/467 and2x2. Shared GeneratedContainerFootprint now validates full catalog width/height and same style across every cell; ordinary mixed-style chests reject too. Failed placement/registry rollback deliberately does not reproduce a phantom orphan record from vanilla's pre-success CreateChest side effect; no partial object is published.
- Source/tests prove all13 ordinary arrangements in this cleared-room slice, not full furniture runtime semantics or Underworld parity. Focused63 and affected87 cases passed. Negative controls: omitting real furniture invocation fails all3 canonical maps; checking only two dresser columns fails2 corruption tests. Both restored before final gates. Paintings/banners, terrain/lava/ore equality and special seeds remain open.

## Underworld lighting and ash vegetation continuation

- Source: ordinary WorldGen.Underworld before AddHellHouses; GrowTreeSettings.Tree_Ash (nested in WorldGen), GrowTreeWithSettings, IsTileTypeFitForTree, EmptyTileCheck; AddHellHouses torch loop and PlaceTile/TileFrameImportant/CheckTorch, official1.4.5.8. Grass scans x25..<width-25 only at strict x<width*.17 or x>width*.83, yheight-300..<height-100+Next(-1,2); condition redraw includes termination. Active57 with any inactive eight-neighbor becomes633. Subsequent yheight-200..<height-50 scan tries634 above633 with inactive cell above and Next(3)==0.
- Tree_Ash requires633 soil, DefaultTreeWallTest, height7..12, padding4, tree634/sapling20; clearance spans all five columns. Segment/branch atlas reuses ordinary trees, but root algorithm is different: independent Next(3) removals left then right even when ineligible; root soil uses generic IsTileTypeFitForTree. Right root variant precedes left; base variant always drawn. Settings base frameX both88/left0/right66; crown leafy unless Next(13)==0, then variant Next(3). Do not substitute ordinary GrowTree's root normalization. Runtime and special-seed growth remain unadmitted.
- Torch requests200*(width/4200) use integer division first (Small200/Medium200/Large400), max1001 samples/request. Samples x[.2width,.8width), y[height-300,height-20); active75/76; left wall wins over right even when blocked. Target and below inactive; existing active4 exclusion is [sampleX-8,sampleX+8) and same y, NOT centered on target. Wet style7 rejection occurs before anchor cleanup but finishes the request. CheckTorch runs for frame-important tiles during generation; below inactive makes left/right/wall frames22/44/0 with frameY154. Respect tileNoAttach/beam/tree sets and directional slopes. Unknown active attachment types fail closed.
- New scripted lighting27 and vegetation20 tests plus canonical3-size assertions pass; negative controls omitting each production invocation independently cause3 failures, restored. These facts do not close Underworld terrain, furniture, paintings, banners, special seeds or whole-pass RNG equality.

## Underworld fort and forge structure slice

- Source: WorldGen.AddHellHouses, HellFort and HellFort_* helpers, PlaceTile/PlaceDoor/Place3x2, SolidTile/SolidTile2, Tile.Clear/ClearTile in official1.4.5.8. Ordinary anchors scan x100..width-100 within the inclusive middle half; y starts height-40 and rises through active/wet cells, requiring active foundation. Palette draws Next(75,77), then Next(5)>0 forces75; pairs75/14 and76/13. After each admitted anchor x adds Next(30,130), then optional Next(0,200) on Next(10)==0.
- HellFort draws six column widths (center halves4..9, side widths8..19) and ten heights6..11. Two left/right wing rounds consume clamped Next(10); the subsequent Next(10) is a zero/nonzero branch, never decremented while extending to the row boundary. Existing walls at each left edge below UnderworldLayer suppress that column before the center column is set. Two overwritten Next(10) draws remain observable; no-wing center expands until last-first>=5. Rooms above height-200 or below height-20 are excluded.
- Interior links use random row selection and Obsidian doors style19, with independent Next(3)*18 frameX per cell and frameY1026/1044/1062. Platforms style13 have frameY234 and source span difference2..5. Exterior doors need three dry inactive neighboring cells. Crumbling uses Next(2), outside wall/liquid/SolidTile gates, NextFloat plus Next(2), and Tile.ClearTile (which preserves inactive type/frames/paint/wires). Torch/furniture/painting phases are NOT implied by this structural helper.
- Hellforges attempts width/200 placements with Next(1,width), Next(height-250,height-30); sampled wall13/14 gates the downward active-cell search. Only failed house placements count toward10000, not non-wall samples. Place3x2(77) writes six cells with18-pixel frames, requires all three SolidTile2 supports, allows full platforms but not table-only tiles, and does not reject or erase liquids. Outer PlaceTile clears Tile|TilePaint|Slope on its inactive anchor even on object rejection; other footprint cells retain paint/coatings. Generation rejection guards fail the isolated candidate instead of inventing substitute geography.
- Runtime pipeline previously omitted AddHellHouses entirely and placed forges on arbitrary ash floors with12/18/24 quotas. The new structural helper stays in existing MidPipeline.Underworld; SourceBackedSurfaceFinish owns source forge scheduling. Ordinary ash vegetation and lighting are added by the continuation above; Underworld terrain/lava/ore runners, remaining settlement decoration and whole-pass RNG equality remain open.

## Eye of Cthulhu death rewards and persistence

Runtime extension boundary, not a vanilla fact: server-controlled players have no Terraria client to consume packet90. The aggregate player snapshot lookup does include them, so passing them straight to the existing addressed item sink caused a lethal-hit exception. Difficulty-loot recipient projection now requires an exact-generation playing/open network consumer and otherwise fails closed for personalized rewards, without removing combat interaction or redirecting rewards to observers. Classic world drops remain unchanged. Direct inventory grants/clientless Master pet entitlement require an explicit actor-owned policy; none is invented here.

- RegisterBoss_EOC NPC4 orders BossBag3319, MasterModeCommonDrop4924, MasterModeCommonDrop3763 (Aviator Sunglasses, not an optional pet), MasterModeDropOnAllPlayers4798(1/4), NotExpert mask2112(1/7), binoculars1299(1/40), UnholyArrow47(20..50), then Crimson ore880(30..90)/seeds2171(1..3) or Corruption ore56(30..90)/seeds59(1..3). Later trophy1360 is1/10. Conditions read the authoritative world evil, not player biome. Common rules consume luck and stack, per-player bags/pets retain raw RNG.
- Source dimensions:47=10x28;59/2171=14x14;1299=14x28;1360=30x30;2112=28x20 (group2104..2113);3319=24x24;3763=18x18;4798=16x30;4924=14x14. None has a natural prefix; ranged ammo47 is not in PrefixLegacy weapon item sets and is not an accessory. Existing ore definitions are reused.
- NPC death case4 calls SetEventFlagCleared(ref downedBoss1,13). WorldFile writes/reads crimson, downedBoss1, downedBoss2 in that exact order. The runtime now marks its existing EyeOfCthulhu journal milestone in all three shared combat death branches and patches just the owned downedBoss1 byte. Full-file decode and repeated-header-patch tests prove persistence/idempotence; generic global event announcement/network parity is not claimed by this slice.

## Queen Slime and mechanical NPC-specific death loot

Mechanical continuation: RegisterBoss_SkeletronPrime127 uses bag3327/relic4933/pet4805, Classic mask2107(1/7), HallowedBar1225(15..30), Soul547(25..40). Destroyer134 uses3325/4932/4803/2113/1225/548. Twins125/126 shares MissingTwin-gated bag3326, Classic mask2106/bar1225/soul549 and Master4931/4804; MissingTwin tests NPC.AnyNPCs of the opposite eye (active, not a guessed life threshold). Later trophies1368/1369 are independent per eye, even on the first death; Prime1367/Destroyer1366 use the boss trophy condition. NPC.ApplyInteraction propagates across active125/126 or127..131 before the strike; existing ledger stores exact live generations. Soul547/548/549 is explicitly in ItemID.Sets.ItemNoGravity, so Item.NewItem's Y RNG is Next(-30,31), not Next(-40,-15). Masks2106/2107/2113 use the grouped28x20 defaults; souls18x18, bar20x20, trophies30x30, bags24x24, pets16x30, relics14x14. MechdusaKill additionally depends on SpecialSeedFeatures.Mechdusa and absence of other mechanical types before item5382: that feature/prefix slice is not admitted.

- ItemDropDatabase.RegisterBoss_QueenSlime (NPC657): BossBag4957; MasterModeCommonDrop4950; MasterModeDropOnAllPlayers4960 with denominator4; NotExpert children in order Common4986(1,25,75), Common4959(7), OneFromOptions(1,4982,4983,4984), Common4758(4), Common4981(4), NotScalingWithLuck4980(3). The later boss-trophy registration adds4958 at1/10 in every difficulty. CommonDrop consumes RollLuck then stack; armor consumes RollLuck then option only; hook and per-player pet chance use raw RNG. Master relic still consumes RollLuck(1). Global coins/hearts and opening bags are outside this slice.
- Blade Staff4758 SetDefaults: damage6, knockBack0, width26/height28, summon. PrefixLegacy includes it in the22-entry Summon family. Item.TryGetPrefixStatMultipliersForItem rejects unchanged rounded damage and non-unit knockback on zero base knockback: only87/88/92/95/96/53/40 and no-prefix remain. Prefix(-1) performs no-prefix, family, reduced-natural-chance and validity-reroll before world-drop velocity RNG. Catalog admission here is world-drop-only, not projectile864/minion combat support.
- Queen Slime source dimensions:4950=14x14;4957=24x24;4958=30x30;4959=18x18;4960=16x30;4980=18x28;4981=10x32;4982/83/84=18x18;4986=18x20. Defaults/helpers and global CommonMaxStack9999 are source-pinned. Tests cover all12 entries, exact RNG order, seven valid prefixes, negative gates and real delivery via the existing server-player combat boundary.

## Ordinary palm generation

- Ordinary `PlantCactus` calls GrowCactus once, then150 pairs Next(x-1,x+2), Next(y-10,y+2) even after initial rejection. GrowCactus accepts nactive80 or Conversion.Sand53/112/116/234, rejects half bricks/wet cell above; scans [x-50,x+50),[y-25,y+25) clipped by InWorld and rejects integer sum/255>25. Root scan x±6/y-3..y+1 excludes five-tile borders, stops as soon as sandCount>10, rejects the fourth cactus encountered before that point. Generation consumes Next(2) to flatten support; CheckCactus rejects remaining slopes. Existing cactus traces the supporting trunk, counts the five-wide/bottom-minus11 envelope, caps against Next(11,13), then source-ordered Next(3)/Next(2,8) grows trunk/arms. Paint/coatings propagate. TileFrame calls CheckCactus but skips TileFrameCosmetic/CactusFrame while generatingWorld: zero cosmetic frame fields during generation are legitimate, unlike missing palm crowns. Special-seed/runtime growth and mixed-pass density remain open.

- `WorldGen.GrowPalmTree` accepts active flat sand types 53/112/116/234, a dry base and `DefaultTreeWallTest`. It skips exact sapling20 downward, requires the two base cells plus a three-wide 20-high upper envelope via `EmptyTileCheck(...,20)`, and only then draws height Next(10,21), bend Next(-8,9)*2. Base frameX66, intermediate Next(0,3)*22, crown Next(4,7)*22; frameY stores two-pixel bend steps after the first quarter. The source bend condition ends in `|| true`, but preceding short-circuit Next(13)/Next(9)/Next(5) calls still consume genRand. Ground paint/block coatings propagate. This is ordinary generation only; runtime growth/secret seeds are not admitted by this helper.
- The ordinary `Cactus, Palm Trees, & Coral` source pass scans y from world top while y<worldSurface-1, not a narrow window around the layer marker. The former window missed dry beaches in canonical seed42. Density/oasis/cactus/coral placement are still partial, not source-exact whole-pass RNG parity.

## Duke phase-one/two cycle continuation

- AI70 getRect extends left/top by40+halfBody but dimensions only by80; it is asymmetric and integer-based, checking every active&&!dead player. Proximity sets ai0=1/ai1=4 then decrements immediately. NPC.checkDead type371 restores lifeMax/DontTakeDamage via PrepareForDeathAnimation with the same state. While justHit/detonating, physical width/height become100 around the old center and EncourageDespawn(3). Original physical size36 does not follow visual ai3 scale. Target255 alone initializes scale Next(80,121)/100, speed Next(165,265)/15 and two Next(-100,101) offsets; assigned stationary children do not relaunch. Homing precedes two Next(-10,11) jitter draws and wind. WorldFile.LoadWorld reads saved windSpeedTarget and initializes windSpeedCurrent identically; runtime currently retains that value, not full Main.UpdateWeather evolution.

- AI69 state2 emits NPC371 at incoming ai2 multiples of4, including0, before incrementing the80-tick clock. Source NewNPC has default target255, velocity0 and ai[]0. Mouth bottom=(int)(center+normalize(player.Center-center)*85), with integer Y+45. State7 similarly emits over120ticks from normalized incoming velocity; child target is assigned, velocity is incoming direction rotated +Pi/2*direction with magnitude6, and ai3=Next(80,121)/100. Only then does the parent circle velocity rotate. AI70 increments lifetime only when ai0==0; at150 it enters ai0=1, ai1=4 and decrements in the same tick. Subsequent ticks count3->2->1->0, then life0/activefalse without StrikeNPC loot. Runtime maps that terminal state to the existing generation-safe TimeLeft0 expiry pass. AI70 initialization, jitter, persisted wind input, proximity and physical expansion are now implemented; live weather evolution remains open.

- AI69 state0 selects dash1 for ai3 0..9, bubble2 for 10 (reset ai3=1), shark3 for 11 (reset0); half-life transition selects4 only at the hover timer boundary. State5 selects dash6 for 0..5, circle7 for6 (reset1), shark8 for7 (reset0); phase-three transition9 is likewise boundary-owned. State7 exits after120 preserving its cycle; state8 after90 preserving its cycle. Circle7 and13 rotate incoming velocity by -Tau/60*direction each tick. Hover acceleration applies an additional step when reversing an axis and does not clamp to desired speed. Source phase-two hover parameters differ between dash and special-attack cycles. Bubble spawning/AI70 and ocean/enrage were audited but are not completed by this slice.

## TZ-35 targeting and mirror evidence

- `NPC.SetDefaults` resets friendly=false, chaseable=true, immortal=false. Source exceptions are pinned in `VanillaNpcChaseability1458`; in particular clone 440 and Ancient Doom 523 are unchaseable. `NPC.CanBeChasedBy` requires active, chaseable, lifeMax>5, !friendly, !immortal (ordinary debug settings), and !dontTakeDamage unless explicitly ignored. The runtime additionally excludes dead/catchable candidates conservatively; temporary catchable immunity remains incomplete.
- `AI_069_DukeFishron`: executed states 10/12 set chaseable=false, 11 sets true, other branches preserve it. Do not derive flags from the *resulting* ai[0]: the transition tick retains the old branch's flags.
- AI69 ordinary phase-three state10 uses horizontal hover offset 360 and selects dash11 for cycle 0/2/3/5/6/7, teleport12 for 1/4/8. State12 teleports only at incoming ai[2]==15: resolve ai[1]=300*sign(npc.Center.X-player.Center.X) when zero, then center=player.Center+(-ai[1],-200). Velocity multiplies by .98, then Y lerps toward zero by .02. Its outgoing 30-tick boundary increments the cycle and wraps at 9. State10/11 alpha changes by +25/-25, state12 by +17. Ocean/enrage and other phase details are not covered by this slice.
- `AI_084_LunaticCultist`: intro is unchaseable/invulnerable; movement state 1 is invulnerable but does not set the chaseability gate; ritual state 5 is unchaseable throughout and invulnerable while its incoming timer is below 120. Timer 119->120 is still invulnerable; 419->idle remains unchaseable for that tick. Ritual-hit abort occurs before branch evaluation.
- `Item.SetDefaults(50)`: Magic Mirror, width/height 20, useStyle=4, useTime/useAnimation=90. `Player.ItemCheck` invokes recall when itemTime==useTime/2. `Player.Spawn_SetPosition` maps floor tile to (x*16+8-width/2, y*16-height). Packet 12 transmits SpawnX/Y and PlayerSpawnContext.RecallFromItem=2 before the observer calls Spawn. Bot follow relocation is custom server policy, not a claim that vanilla mirrors target players; its recall presentation supplies the bot's landing floor instead of retaining an unrelated observer spawn.
- `Player.PickAmmo`: Magic Quiver scales arrow launch speed by 1.1. Random loadout tests must account for the equipped accessory, not assume every Platinum Bow + Unholy Arrow launches at 10 rather than 11.

This file stores concise facts already checked against the locally decompiled official TerrariaServer **1.4.5.8**. It prevents repeated source archaeology, but it does not authorize guessing adjacent behavior. Unknown cases remain fail-closed until separately verified.

## Runtime bot source facts

Primary evidence: TerrariaServer 1.4.5.8 `Player.QuickHeal`, `Player.QuickHeal_GetItemToUse`, `Player.UpdateBuffs`, `Player.ApplyEquipFunctional`, `Player.HorizontalMovement`, `Player.Update_NPCCollision`, `Player.PickAmmo`, `Player.WingMovement`, `Item.SetDefaults`, `Projectile.SetDefaults`, `Projectile.AI_001`, `Collision.CanHit`, `MessageBuffer.GetData` packet cases 13/23/30/55 and `NetMessage.SendData` packet cases 13/23/30/55.

Verified facts used by the TZ-29 bot slice:

- `Player.defaultItemGrabRange` is `42` pixels before accessory modifiers. Bot pickup uses that base range and does not invent accessory bonuses.
- ordinary potion delay is `Item.potionDelay = 3600` ticks. `Player.QuickHeal` refuses healing while dead, full-life or already under potion delay.
- QuickHeal candidates require `stack > 0`, `type > 0`, `potion == true` and `healLife > 0`. Its candidate ordering prefers the largest still-underhealing potion while all candidates under-heal, otherwise the smallest non-negative overheal. The special Restoration Potion type `227` adjustment is not part of the admitted bot healing subset.
- the source-pinned healing subset used by bots is Healing Potion `188` / `100 HP`, Greater Healing Potion `499` / `150 HP`, and Super Healing Potion `3544` / `200 HP`; each has the ordinary potion flag and a `14x24` item hitbox. Absence from the catalog is fail-closed.
- packet `30` encodes `[player byte][hostile bool]`. On a dedicated server an incoming player index is replaced with the sending connection's player slot before the authoritative hostile flag is stored and rebroadcast.
- packet `55` (`AddPlayerBuffPvP`) encodes `[player byte][buff ushort][time int32]`. The dedicated-server ingress path admits network PvP buffs only when the target and sender are both hostile and `Main.pvpBuff[buff]` is true. On a multiplayer client the packet is applied only when the encoded player slot equals `Main.myPlayer`; vanilla therefore uses packet `55` as targeted local-player PvP buff delivery, not as observer-visible remote-player buff replication. Server-owned fake-player bot combat buffs must not be broadcast to unrelated observers through packet `55`.
- Archery buff `16` sets the archery state and multiplies arrow damage by `1.1`; `Player.PickAmmo` additionally multiplies arrow speed by `1.2` when below `20`, capped at `20`.
- Wrath buff `117` adds `0.1` to melee, ranged, magic and minion damage multipliers. TZ-29 reproduces only the outgoing combat effects it actually owns; catalogued potion buffs without an implemented authoritative effect remain fail-closed for bot pickup/use.
- supported bot ammunition/weapon facts come from the existing source-backed projectile weapon/ammo catalog. TZ-29's ranged presets pair Wooden Bow with Wooden Arrow and Musket with Musket Ball; it does not infer arbitrary Terraria ammo compatibility.
- Copper Broadsword is item `3508`, Wooden Bow is item `39`, Musket is item `96`, Wooden Arrow is item `40`, and Musket Ball is item `97`. Their admitted damage, use timing, projectile and ammo conversions come from the existing 1.4.5.8 item/weapon catalogs rather than bot-local guesses.
- Wooden Bow plus Wooden Arrow resolves through `Player.PickAmmo` to launch magnitude `9.1`; Musket plus Musket Ball resolves to `13`. aiStyle-1 arrow gravity begins after 15 projectile substeps at `+0.1` vertical velocity and caps at `16`; projectile `extraUpdates` controls the number of substeps per game tick.
- Packet 13 control bits are up `0`, down `1`, left `2`, right `3`, jump `4`, use-item `5`, direction-right `6`, and pulley/dash `7`. Movement flag bit `2` carries velocity. A server-owned PlayerBot must publish the actual selected hotbar slot and use-item bit for observers to render weapon switching/use.
- Fishron Wings are item `2609` with wing slot `26`; Soaring Insignia is item `4989`, Terraspark Boots are `5000`, Magiluminescence is `5107`, and Master Ninja Gear is `984`. Terraspark sets `accRunSpeed=6.75`, enables rocket boots and adds `0.08` to move speed. Grounded Magiluminescence multiplies run acceleration and slowdown by `1.75` and both run-speed caps by `1.15`. The admitted bot physics uses these exact horizontal parameters plus the existing Fishron-wing vertical slice; Master Ninja dash and other accessory side effects remain fail-closed.
- `Player.Update_NPCCollision` on the client skips only inactive, friendly or non-positive-damage NPC state, but packet 23 does not transmit a per-instance `friendly` or `damage` field. A hostile-type presentation actor with server-only `DamageOverride=0` must therefore remain spatially separated from the followed player to avoid local contact-hurt presentation; authoritative contact damage still resolves the stored override and remains zero.
- `Collision.CanHit` is a tile-aware rectangle visibility test. Bot Guard acquisition and projectile admission reuse the runtime's exact `VanillaWorldCanHit` port; a solid obstacle is not ignored merely because target range is valid.
- AI_002 (`FloatingEye`) performs collision rebound before target-direction steering and applies source-specific horizontal/vertical pursuit acceleration. The controlled NpcBot lane reuses only this verified steering/motion primitive; daylight/despawn/attack side effects remain outside actor-control unless separately admitted.
- AI_005 (`EaterOfSouls`) resolves a target and then applies source-specific pursuit velocity/collision behavior. Controlled flyers reuse the verified pursuit primitive but do not opt back into ordinary AI projectile or spawn side effects.
- ordinary AI_014 bats set no-gravity, rebound from `collideX/collideY`, call `TargetClosest`, apply directional pursuit acceleration, then advance `ai[1]`; the wander branch starts only after `ai[1] > 200`. TerraRuntime's controlled bat helper resets that ordinary AI clock for each call and therefore exposes the source-ordered pre-wander pursuit/collision slice only. Queen Slime's purple minion shares AI_014 machinery but is boss-owned and is explicitly excluded from the standalone bot-preset helper.

## Larva destruction source facts

Primary evidence: TerrariaServer 1.4.5.8 `WorldGen.Check3x3`, `WorldGen.KillTile`, `Projectile.CutTiles` and `Projectile.ExplodeTiles`.

- Larva is frame-important tile `231` and resolves as one complete 3x3 object with 18-pixel frame steps. A corrupt/incomplete footprint is not a valid object mutation target.
- After Larva destruction, `WorldGen.Check3x3` scans live, non-dead players in slot order, minimizes Manhattan distance from the tile source and calls `NPC.SpawnOnPlayer(player, 222)` only when the nearest distance is strictly below `4800` pixels.
- Tile `231` is in the final `tileCut` set and is not excluded by `Projectile.CanExplodeTile`; admitted server projectile cutting and trusted terrain explosions therefore reach the same framed-object/Queen Bee authority boundary as an accepted pick break.

## Dungeon entrance source facts

Primary evidence: TerrariaServer 1.4.5.8 `DungeonCrawler.SetupDungeonDataVariables`, `DungeonUtils.SetOldManSpawnAndSpawnOldManIfDefaultDungeon`, `WorldGen.beachDistance`, and the final `TileID.Sets.Clouds` initialization.

- An ordinary precalculated entrance search starts from y `10`, scans downward while the tile is inactive and has neither liquid nor wall, and accepts only x strictly inside `WorldGen.beachDistance == 380` from both world edges.
- The search initializes its countdown to `3000`, decrements before evaluating a candidate and stops at zero, so it can evaluate at most `2999` candidates. Each candidate x is drawn within 100 tiles of the Reset-owned dungeon location.
- Acceptance rejects a cloud-set tile within radius `15` of the surface exit or within radius `50` around `max(50, y - 50)`, and requires `y - 40 - RoughHeight > 0`. The final 1.4.5.8 cloud set used by this check is tiles `189`, `196`, `460`, `717`, `718`, and `719`.
- After acceptance, the horizontal dungeon location receives the source `+25-Next(50)` adjustment while the entrance position remains the accepted surface coordinate.
- For the default dungeon, Old Man is NPC type `37` at `dungeonX * 16 + 8, dungeonY * 16`, with `homeless=false` and home tile set to the dungeon anchor.

## Underground house source facts

Primary evidence: TerrariaServer 1.4.5.8 `WorldGen` pass `Underground Houses and Buried Chests`, `CaveHouseBiome`, `HouseUtils`, `HouseBuilder`, the seven house palette builders, `StructureMap`, `SetFactory`, and `WorldGenRange`.

- The pass draws its budgets before placement in this order: area-scaled `CaveHouseCount 35..40`, width-scaled `UnderworldChestCount 10..15`, area-scaled `CaveChestCount 35..40`, then area-scaled fixed `AdditionalDesertHouseCount 2`. Placement then runs cave chests, Underworld chests, ordinary cave houses and additional desert houses.
- Cave-house room search scans down by at most `200`, searches each side by `25` and upward by `10`, clamps room width to `15..30` and height to `8..12`, and admits the optional upper/lower rooms through their solid percentage plus `0.2` tests.
- `StructureMap` rejects the exact source tile set `225, 41, 43, 44, 226, 203, 112, 25, 151, 21, 467`; non-Granite houses also reject lava in the padded rooms. All ordinary `CaveHouseBiome` chest chances are `1.0`, so an admitted house without its configured persistent chest is invalid.
- The palette defaults used by the current structural slice are Wood `30/27/124/0/0/chest 21 style 1`, Ice `321/149/574/19/30/chest 21 style 11`, Desert `396/187/577/42/43/chest 467 style 10`, Jungle `158/42/575/2/2/chest 21 style 8`, Mushroom `190/74/578/18/6/chest 21 style 32`, Granite `369/181/576/28/34/chest 21 style 50`, and Marble `357/179/561/29/35/chest 21 style 51`; each tuple is tile/wall/beam/platform/door/chest. Decorative furniture and aging are not implied by these palette facts and remain unported.

## Liquids

Primary evidence: `Terraria.Liquid.Update`, `Terraria.Liquid.LiquidCheck`, and the relevant liquid helper/check paths in TerrariaServer 1.4.5.8.

Verified runtime facts currently implemented/tested:

- Ordinary same-kind settling is gravity-first, then horizontal leveling when liquid remains in the source during the same update.
- Horizontal leveling uses the source-backed 2/3/4/5/7-cell averaging shape.
- The full-source downward-fill edge preserves the vanilla `255 -> 254` source case when the lower cell receives the final unit.
- 5/7-cell averaging preserves the fed source column in the verified branch where neighbours already equal the rounded level.
- Lava ordinary flow delay is 5 liquid updates.
- Honey ordinary flow delay is 10 liquid updates.
- Water does not own the material merge itself; it wakes adjacent lava/honey/shimmer cells and the foreign-liquid update owns `LiquidCheck` merge placement.
- Water + lava creates Obsidian tile `56`.
- Water + honey creates Honey Block tile `229`.
- Lava + honey creates Crispy Honey Block tile `230`.
- Shimmer contact wins the verified source-order selection and creates Shimmer Block tile `659`.
- The verified ordinary open-cell merge threshold is 24 liquid units.
- Left/right/up foreign-liquid handling and the lower-cell sub-24 source clear follow `LiquidCheck` ordering.
- Material mutations and the verified lower-cell sub-24 foreign-liquid clear use packet `20` tile-square replication, not packet `48` alone.
- `Liquid.maxLiquid` is `25000`. On an ordinary dedicated server, `UpdateLiquid` counts active players only in slots `0..14`, sets `cycles = 10 + activePlayers / 3`, and sets `curMaxLiquid = 25000 - activePlayers * 250` unless the reduced-max-liquid setting forces `5000`. Each cycle processes the corresponding `curMaxLiquid / cycles` slice, with the last cycle extending to the current active count.
- `LiquidCheck` clears participating merge liquids before `CreateLiquidMergeTile`; the material merge is then synchronized by the tile-square packet rather than separate packet-48 updates for every cleared merge cell.
- TerrariaServer 1.4.5.8 packet-20 `TileChangeType` values for these merges are `LavaWater=1`, `HoneyWater=2`, `HoneyLava=3`, `ShimmerWater=4`, `ShimmerLava=5`, `ShimmerHoney=6`.
- the final initialized `Main.tileObsidianKill` set contains 276 tile types; TerraRuntime stores the exact source-pinned set instead of deriving it from a guessed category.
- `TileID.Sets.IsAContainer` for this path is exactly tiles `21`, `467`, and `88`.
- for non-water lower contact, `LiquidCheck` performs the verified `Main.tileCut` lower-cell kill before lower merge eligibility is evaluated.
- In dedicated-server `Liquid.UpdateLiquid`, stable active entries retire at `10 + activePlayersInSlots0To14 / 3`; Terraria 1.4.5.8 counts only player slots `0..14` for this liquid-cycle threshold.
- A changed liquid amount resets `kill` to zero and schedules the cell above; unchanged entries increment `kill`.
- A stable `254` liquid amount is normalized to `255` when the active entry retires.
- Water with `y > Main.UnderworldLayer` loses two liquid units per `Liquid.Update`; `Main.UnderworldLayer == Main.maxTilesY - 200`.
- While `WorldGen.isGeneratingOrLoadingWorld`, `Liquid.quickSettle` implies quick-fall scheduling: lava/honey delay gates are bypassed, the active-entry retirement threshold is the fixed value `8`, and a source left above `250` after downward transfer is restored to `255`.
- Loading-time `LiquidCheck(..., createMergeTilesDuringGen: false)` clears participating foreign liquids but does not create Obsidian/Honey/Crispy/Shimmer merge blocks.
- `Liquid.QuickWater` scans bottom-up with default y bounds `3..maxTilesY-3` and x bounds `4..maxTilesX-5`; `tilesIgnoreWater(true)` temporarily makes tiles `138`, `484`, `546`, `664`, `711..716` non-solid, while tile `379` (Bubble) is explicitly solid.
- `WorldFile.LoadWorld` invokes `Liquid.QuickWater(2)` where `2` is verbosity, not a y-coordinate or scan bound; the normal-world load therefore runs the complete QuickWater scan.
- The normal 1.4.5.8 post-load liquid sequence is `QuickWater -> WorldGen.WaterCheck -> quickSettle UpdateLiquid until the active queue is empty or 100000 iterations are reached -> WorldGen.WaterCheck`. Reaching the 100000 guard does not fail loading; vanilla continues to the final WaterCheck.
- Correction (2026-09-07): `TileObjectData` constructor initializes `_useGlobalLiquidChecks=false`; `ApplyNaturalObjectRules` sets it true. `CheckWaterDeath(Tile)` / `CheckLavaDeath(Tile)` first resolve the instance's frame-selected subtile and alternate. Only absent/global-check data use Main's raw tables. The earlier always-global claim was incorrect.
- The final 1.4.5.8 `tileWaterDeath` set contains 10 tile types and the final `tileLavaDeath` set contains 267 tile types. Loading-time supported liquid-death removal is itemless because the world is still in the generating/loading state.

Known liquid gaps that must not be guessed:

- complex `WorldGen.ReplaceTile` dependency/shape/actuator/object cases beyond the currently supported safe single-cell active-merge subset;
- Remix/Zenith load-time liquid remapping until the generation-only lava-line/ocean-depth inputs consumed by `SettleWaterAt` are represented in the load context;
- panic/forced-settle lifecycle beyond the verified loading quick-settle scheduler slice.

## Cross-world spawn echo

Primary evidence: TerrariaServer 1.4.5.8 `Player.Spawn(PlayerSpawnContext)` and `MessageBuffer` packet-12 handling.

Verified facts used by the live transfer gate:

- when `Player.Spawn` runs with `PlayerSpawnContext.SpawningIntoWorld`, it calls `FindSpawn` and validates the personal spawn; an invalid personal spawn may leave `SpawnX/SpawnY` as `-1/-1`;
- a multiplayer client sends packet 12 back after that spawn transition;
- therefore the immediate packet-12 `SpawningIntoWorld` response to TerraRuntime's synthetic replacement-world spawn is an acknowledgement/echo of the handoff, not a fresh authoritative respawn request;
- ordinary post-join packet-12 respawn remains a separate path and must not be globally suppressed.

## Mining, drills and join inventory ordering

Primary evidence: TerrariaServer 1.4.5.8 `MessageBuffer.GetData` case `3`, `NetMessage.SyncConnectedPlayer`, `Mount.UseDrill`, `Mount.drillPickPower`, `Player.PickTile`, and `ItemID.Sets.IsDrill`/item defaults.

- On receipt of the client slot assignment (packet 3), the vanilla multiplayer client sends all 59 ordinary inventory packet-5 entries before it sends packet 6 requesting world data. Pre-world-request inventory is therefore normal join ordering, not malformed gameplay traffic.
- Packet 13 carries `selectedItem`, `controlUseItem` and mount state; the client drill/pick path ultimately emits ordinary packet-17 tile manipulation.
- Drill Containment Unit is mount type `8`. `Mount.UseDrill` requires the drill ability to be active and `controlUseItem`, uses static `drillPickPower = 210`, and calls `Player.PickTile` for its selected beam target. The mount ability-active bit itself is not represented by packet 13, so TerraRuntime's admitted server slice additionally requires the DCU summon item `2768` in ordinary inventory and otherwise fails closed.
- Source-backed drill IDs needed by the current pick catalog include Cobalt Drill `385`, Nebula Drill `2779`, Solar Flare Drill `2784`, and Stardust Drill `3464`; the latter three inherit pick power `225` through the shared defaults branch.
- `Player.BordersMovement` reserves a 640-pixel edge band in normal-sized worlds. A mining regression whose movement sample sits inside that band has not proved selected-slot/control state reached the authoritative player; production-order tests must use an accepted position and assert the committed packet-13 snapshot before packet 17.

## Explosive projectiles and terrain destruction

Primary evidence: TerrariaServer 1.4.5.8 `Item.SetDefaults`, `Player.PickAmmo`, `Projectile.SetDefaults`, `Projectile.AI_016_Bombs`, `Projectile.AI_147_Celeb2Rocket`, `Projectile.Kill_ExplodeTiles`, `Projectile.ShouldWallExplode`, and `Projectile.CanExplodeTile`.

- Celebration Mk2 is item `3930`: damage `50`, knockback `10`, shoot speed `17`, use time/animation `6`, rocket ammo and holder projectile `714`. `Player.PickAmmo` transforms its rocket-ammo result as `715 + ammo.type - AmmoID.Rocket` rather than the ordinary launcher offset.
- Holder `714` is `22x22`, non-friendly, non-hostile, non-tile-colliding, ranged presentation/channel state. Children `715..718` are `14x14`, friendly ranged aiStyle `147`, penetrate `1`, timeLeft `1080`, tile-colliding and run with `extraUpdates = 2`. Their seven-pattern sequence emits one child except pattern 4 (two) and pattern 5 (three); pattern 3 launches at speed `9`, the others at `8`.
- Bomb `28` and Dynamite `29` are aiStyle `16`. The admitted Mini Nuke defaults cover `793..801` and `803..810` (`802` is not inferred): `14x14`, friendly ranged, penetrate `-1`, ordinary default lifetime and tile collision. Exact type membership, not aiStyle alone, decides the ordinary-fuse/grenade/mine/straight-rocket branch.
- `Kill_ExplodeTiles` is owner-local in vanilla (`owner == Main.myPlayer`) and sends packet 17 for successful tile/wall kills. TerraRuntime instead lets only a provenance-trusted authoritative termination perform that irreversible mutation and treats the matching client packet-17 stream as convergence echo.
- The destruction membership test is strict Euclidean distance `< radius`, not `<=`. Celebration Rocket IV `718` uses center-based radius `5`; Mini Nuke II rocket/grenade/mine `796/797/798` use position-based radius `7`. Celebration Rocket I/III `715/717` have no `Kill_ExplodeTiles` terrain branch.
- `CanExplodeTile` always rejects dungeon tile types (`Main.tileDungeon`), `TileID.Sets.BasicChest`, temple wall `350`, and the exact hard-protected tile switch. It conditionally gates hardmode ores, Demon/Crimson Altar, Underworld Hellstone, and the remaining world-state-dependent cases. Wall removal is enabled only when `ShouldWallExplode` finds an empty wall cell inside the same strict radius; wall `350` is never killed. Unrepresented object/world-state cases remain fail-closed.

## Player buff synchronization and hostile projectile debuffs

Primary evidence: TerrariaServer 1.4.5.8 `Projectile.Damage`, `Projectile.Damage_EVP`, `Projectile.StatusPlayer`, `Projectile.ApplyBuffTo`, `MessageBuffer.GetData` packet cases `3`/`50`/`55`, `NetMessage.SendData` packet case `50`, and `Player.maxBuffs`.

- packet `50` (`PlayerBuff`) encodes `[player byte][zero or more buff ushort][zero ushort terminator]`; `Player.maxBuffs` is `44`. It carries buff **types only**, never durations.
- on dedicated-server ingress, packet `50` discards the claimed player byte and replaces it with `whoAmI`. Each reported nonzero type is mirrored with temporary server-side `buffTime = 60`, the remainder of the arrays is cleared, and packet `50` is relayed to peers. The client also sends packet `50` during the normal join handshake, and `SyncOnePlayer` includes it in late-join player baselines.
- hostile projectile player-status application is not a dedicated-server execution path: `Projectile.Damage()` calls `Damage_EVP` only when `Main.netMode != 2`. A multiplayer client that survives a hostile projectile `Hurt` runs `StatusPlayer` locally and later synchronizes the resulting active buff-type list through packet `50`.
- Cultist Fireball `467` is one concrete example: `StatusPlayer` locally calls `ApplyBuffTo(player, 24, Main.rand.Next(30, 150))`, while `Kill()` expands the projectile to a `176x176` damage area and calls `Damage()` again. The exact PvE debuff duration/RNG result is therefore not transmitted to the dedicated server by packet `50`.
- packet `55` must not be repurposed to manufacture those PvE durations. It is the separate targeted PvP `AddPlayerBuffPvP` path described above; independently rolling a server duration and delivering packet `55` would not be vanilla packet semantics and can extend or alter the client-owned result.
- TerraRuntime packet-50 state is consequently presentation/synchronization evidence only. It may be generation-scoped, relayed, late-join baselined and transferred, but it is not by itself proof that an authoritative combat modifier with a known remaining duration exists.

## World-item reservation and cursor transfer

Primary evidence: TerrariaServer 1.4.5.8 `Main.UpdateServer`, `WorldItem.FindOwner`, `MessageBuffer.GetData` packet cases `21`/`22`, packet-5 inventory handling, and `Player.dropItemCheck`.

- `Main.UpdateServer` calls `FindOwner` for an unreserved active world item when its reservation timer modulo 5 equals 1. `FindOwner` skips inactive/dead players, consults `ItemSpace`/`CanPullItem`, compares candidates by source Manhattan-distance expression (including supported magnet modifiers), and checks grab-range intersection before changing the reservation.
- Server packet-21 handling ignores an existing item when `playerIndexTheItemIsReservedFor != whoAmI`; a different playing client therefore has no vanilla authority to remove or rewrite another player's reserved item. Packet 22 is a server-to-client item-owner projection in this path, not a client ownership grant.
- Packet-5 slot `58` maps to the owning client's `Main.mouseItem`. `Player.dropItemCheck` copies the mouse item into inventory slot 58 and, when the inventory closes, runs `GetOrDropItem`, clears `Main.mouseItem`, and clears inventory slot 58. A cross-world snapshot racing that client transition can therefore neither replay the old cursor image nor guess the full `GetItem`/special-storage semantics.
- TerraRuntime admits the conservative lossless subset: before detach it moves the whole cursor stack only into an empty ordinary main slot `0..49`; if none exists, it cancels the transfer while the source player is still attached. This is an intentional fail-closed policy built from the verified vanilla role of slot 58, not a claim that vanilla `GetItem` itself is limited to empty main slots.


## Projectile-local NPC immunity and controlled-magic reacquisition

Primary evidence: TerrariaServer 1.4.5.8 `Projectile.SetDefaults`, `Projectile.Damage_PVE`, `Projectile.Damage_PVE_Inner`, `Projectile.FindTargetWithLineOfSight`, `Projectile.DecrementLocalImmuneTimeCounters`, and the type-specific post-hit branch for projectiles `34`/`79`.

- Flamelash `34` and Rainbow Rod `79` both set `usesLocalNPCImmunity = true` and `localNPCHitCooldown = 12`; their source penetration is `2` and `3` respectively.
- `Damage_PVE` rejects an NPC while the projectile's corresponding `localNPCImmunity[npcSlot]` cell is nonzero. After a successful ordinary local-immunity hit, `Damage_PVE_Inner` clears the owner's shared `NPC.immune` cell and writes the projectile's `localNPCHitCooldown` into that local cell.
- `DecrementLocalImmuneTimeCounters` decrements only positive local-immunity cells once per projectile update; negative values remain nonzero/permanent. TerraRuntime represents the admitted slice as tick-bound generation-safe state rather than copying a mutable 200-slot array per projectile, but preserves the exact positive cooldown boundary and permanent-negative semantics.
- The source `FindTargetWithLineOfSight` scans physical NPC slots and explicitly rejects any candidate whose projectile `localNPCImmunity[i] != 0`. Target acquisition and collision therefore must consult the same logical immunity state; maintaining separate mirrors can cause a released projectile to immediately reacquire an NPC that vanilla still considers locally immune.
- After a successful hit, projectile type `34` or `79` with exact released state `ai[0] == -1` sets `ai[1] = -1` and requests a net update. On the following AI_009 target search, the just-hit NPC is skipped while its local-immunity cell remains nonzero, allowing another line-of-sight target to be selected. At the positive 12-tick boundary the same exact NPC generation becomes eligible again.
- TerraRuntime scopes each local-immunity cell by both exact `ProjectileHandle` generation and exact `NpcHandle` generation. Reusing either physical slot cannot carry an old local-immunity decision into the replacement entity.

## Projectile PvP status and packet 55

Primary evidence: TerrariaServer 1.4.5.8 `Projectile.StatusPvP`, the PvP branch of `Projectile.Damage()`, `Player.AddBuff`, `MessageBuffer.GetData` case `55`, `NetMessage.SendData` case `55`, and `Main.Initialize` assignments to `Main.pvpBuff`.

- In the PvP damage branch, vanilla checks projectile/player immunity and then calls `StatusPvP(targetSlot)` before `TryDoingOnHitEffects` and before `Player.Hurt(..., pvp: true, ...)`. `Player.Hurt` can subsequently avoid the damage because of Creative GodMode, so a legal PvP hit may still roll/apply its status even when HP does not change. Do not move the status proc after the HP commit merely because that ordering looks cleaner.
- For the currently admitted authoritative projectile set, source-pinned projectile-specific `StatusPvP` rules are: Fire Arrow `2` -> `On Fire!` buff `24`, `180` ticks, chance `1/3`; Flamelash `34` -> `On Fire!` `24`, `240` ticks, chance `1/2`; Poisoned Knife `54` -> `Poisoned` buff `20`, `600` ticks, chance `1/2`.
- `StatusPvP` also contains melee-enchant/equipment-derived effects and many projectile families not currently admitted. Those effects depend on owner state TerraRuntime does not yet own completely and therefore remain fail-closed rather than being inferred.
- `Player.AddBuff(type,time)` on a multiplayer client, when targeting a remote player, sends packet `55` only for a `Main.pvpBuff[type]` entry and does not mutate that remote player's local buff array. Packet `55` is exactly `[player byte][buff ushort][time int32]`.
- Dedicated-server packet-55 handling relays only when both encoded target and sender are hostile and `Main.pvpBuff[buff]` is true. Client handling applies the packet only when the encoded target equals `Main.myPlayer`, using `AddBuff(..., fromNetPvP: true)`. This is targeted owner delivery, not observer replication.
- The exact 1.4.5.8 `Main.pvpBuff` true set is `{20,70,24,323,31,39,44,324,69,103,119,120,137,320,30,36,397,398,399,400}`. TerraRuntime's packet-55 egress must remain bounded to this source-pinned set.

## Working rule for new vanilla facts

When adding a fact:

1. name the official type/method or table used as evidence;
2. state only the behavior needed by TerraRuntime;
3. put literal IDs/constants here only after they are source-pinned and represented by typed production constants where appropriate;
4. add a regression test that fails under the previous/broken behavior;
5. update/remove the corresponding "known gap" entry.

# Паритет ванильной генерации Terraria 1.4.5.8

Приёмка Lakes/Slush завершена для ограниченной обычной матрицы: **31 стадия E9 / 279 checkpoints**, остаётся **43P + 30C** незавершённых строк аудита. Пройдены Release `4908/4908`, восстановленный focused-набор `41/41`, Windows NativeAOT, шесть smoke-проверок и загрузка свежего Small/Classic/Corruption1458 обоими серверами. Полные клетки/RNG и данные туннелей/озёр/снега совпали во всех девяти случаях. Tile L1 `0.196460` и wall L1 `0.458101` на прежнем эталоне проходят неизменные допуски, но не доказывают равенство всего мира. Тест пустого tick теперь прогревает измеряемый цикл без изменения нагрузки и лимита байт; внесённая аллокация по-прежнему проваливает тест, прежние сбои сохранены в agent-memory. Далее — комнаты/коридоры Dungeon внутри существующего графа. Linux NativeAOT, специальные seeds и прохождение клиентом не подтверждены. Предыдущие WIP-абзацы ниже исторические.

Slush включён в пакет Lakes: существующая стадия Ice Biome сохраняет точные границы строк; Slush сканирует их без RNG, преобразуя сохранённые Silt/Stone и Mud только при отсутствии активных материалов джунглей в исходном радиусе. Случайные пятна слякоти удалены. Три официальные проверки всего прохода и focused-тесты границ/сохранения полей прошли. Отрицательные контроли обнаруживают шесть регрессий удержания воды и четыре ошибки проверки активности центра Slush. Три реальных Small-префикса до обеих стадий совпали по клеткам/RNG и таблицам; проверка больших префиксов/полного suite/NativeAOT ещё выполняется. Принятые счётчики аудита остаются29E9,45P+30C до окончания проверок.

Новый WIP Lakes: исходный выбор поверхностных озёр и SonOfLakinater заменяют прежние подземные эллипсы. Существующая стадия Tunnels сохраняет исходные средние колонки для расстояний до озёр; выбор учитывает горные пещеры, пустыню, песок и evil-биомы. Двенадцать официальных проверок формы и шесть полных проверок стадии Lakes совпадают по нормализованным клеткам, следующему RNG и порядку колонок озёр. Существующий рекурсивный рост травы переиспользован при вырезании озёр, а не продублирован; неизвестные материалы, деревья и framing остаются fail-closed. Текущие интеграционные/полные/NativeAOT проверки записаны в agent memory. Статус Lakes пока не повышен; приёмка Corruption ниже предшествует этому блоку.

Текущая приёмка префикса обычного Corruption: девять официальных случаев Small/Medium/Large совпали по полным клеткам и следующему RNG; сохранённые тесты охватывают29 стадий/261 контрольную точку. Стадия32 получила ограниченный E9, незавершёнными остаются45P+30C строк. Полный Release4870/4870, расширенные проверки префикса/травы/пещер85/85, сборка0/0, WindowsNativeAOT+шестьsmoke и загрузка свежегоSmallCorruption обоими серверами прошли. Сравнение с тем же эталоном: tileL1 `0.195909`, wallL1 `0.461801` проходят прежние допуски, но не равны нулю. Реальный префикс Crimson, специальные seed, весь мир и прохождение клиентом остаются отдельными обязательствами. Предыдущие WIP-записи ниже описывают путь к этой приёмке; следующая работа — Lakes.

WIP компонента Corruption: прежние приближённые пропасти и глубокая полоса замены пород удалены. Основные и боковые туннели, повторный поиск алтарей в конце пропасти, общая установка сфер, интервалы между пропастями по колонкам, неглубокое преобразование поверхности и обработка каждой клетки сферы теперь следуют исходному порядку. Тридцать шесть официальных проверок пещер и шесть полных проверок прохода Corruption совпадают по всем нормализованным клеткам и следующему RNG на одинаковом синтетическом входе. Переиспользованы существующие компоненты руды, алтарей, поверхности и framing; альтернативная runtime authority не добавлена. Интеграционная генерация и новая полная/NativeAOT приёмка фиксируются в agent memory. Равенство полного префикса мира и повышение статуса стадии не заявлены.

Приёмка компонента поверхности: `4827/4827` Release-тестов, сборка без предупреждений и ошибок, Windows NativeAOT и шесть smoke-проверок прошли. Новый мир Small/Classic/Crimson1458 загружается в TerraRuntime и официальном сервере 1.4.5.8. Это не подтверждает Linux NativeAOT, прохождение клиентом, равенство всего мира или закрытие стадии. Следующий связанный блок — порядок пропастей, боковых туннелей и сфер обычного Corruption со сравнением с официальным исполняемым файлом до замены существующей аппроксимации.

Расширено доказательство ветки Crimson: шесть полных проверок официального прохода на одинаковом входном рельефе и состоянии совпали по всем нормализованным клеткам и следующему RNG, включая два региона, смесь материалов/стен пустыни, поверхность, поиск алтарей и отложенные сердца. Это не равенство полного префикса генерации мира. Проверок травы теперь шестнадцать, включая существующую паутину; её framing при генерации подтверждён как отсутствие изменений в существующем компоненте. Crimson проходит генерацию на трёх канонических размерах; affected-тесты `94/94`, негативный контроль без поверхности ломает `6/6` проверок. Текущие полные/NativeAOT проверки ещё фиксируются в agent memory.

Новый WIP поверхности: Crimson теперь использует исходный порядок преобразования колонок джунглей, неглубокую полосу со случайным изменением глубины и ограниченный глубиной обход evil-травы на земле/грязи вместо широкой глубокой замены пород. Двенадцать независимых проверок травы на корректно инициализированном официальном сервере совпали по всем полям клеток и следующему RNG; весь порядок поверхности/прохода этим пока не подтверждён. Переиспользуется существующий generation-framing; неизвестные эффекты деревьев/объектов отменяют кандидат, а не аппроксимируются. Геометрия Corruption и её прежний порядок преобразования поверхности остаются открытыми. Текущие проверки нового пакета записаны в agent memory; приёмка алтарей ниже предшествует этому изменению.

Приёмка компонента алтарей: **4804/4804** Release-тестов прошли с подключённой трассировкой аллокаций; сборка без предупреждений и ошибок. Windows NativeAOT, шесть smoke-проверок, свежие Small/Classic/Corruption1458 и Crimson1458 загружаются обоими серверами. Негативный контроль с фиксированным количеством ломает все девять регрессий поздней стадии. Бюджеты сравнения с прежним эталоном проходят: tile L1 `0.196990` (было `0.196269`, немного хуже), wall L1 `0.520040` (было `0.523785`). Монотонное улучшение всего мира, закрытие стадии, Linux NativeAOT и прохождение клиентом не заявляются. Изоляция allocation-тестов сама по себе не устранила нестабильность: после неё тест пустого tick ещё падал, затем трассируемый полный прогон прошёл; точный стек проблемной аллокации не подтверждён.

Изоляция измерений: классы тестов прогретых аллокаций NPC-dispatch и server-player выполняются в непараллельной коллекции. Нагрузки, прогрев и лимиты байтов не изменены; параллельная генерация канонических миров не должна нарушать условия измерения прогретого общего пула. До изоляции два полных прогона упали на разных allocation-проверках, тогда как отдельный набор NPC/алтарей прошёл. Это не доказывает регрессию аллокаций production или её точный стек. Текущая полная перепроверка фиксируется ниже и в agent memory.

WIP алтарей (2026-09-08): существующая поздняя стадия Altars теперь использует обычный расчёт количества по площади, исходный порядок выбора и отбраковки координат, исключение океана и сохранённого положения Shimmer, общие generation-only правила `Place3x2(26)`. В регионах Crimson добавлен поиск алтарей перед отложенными сердцами. Прямое сравнение с официальной операцией покрывает все $18096$ комбинаций материала, формы, актуатора и стиля; девять независимых проверок поздней стадии совпали по всем клеткам и следующему RNG. Сама геометрия Shimmer, полное преобразование поверхности evil-биомов и геометрия Corruption остаются открытыми. Это доказательство компонентов, не закрытие стадии и не паритет всего мира; текущая приёмка записывается в agent memory.

`terraruntime:flat` остаётся отдельным минимальным детерминированным генератором. Работа над ванильным worldgen ведётся только внутри уже существующего `terraruntime:vanilla`.

## Схема постепенного переноса

Приёмка компонента пещер Crimson: **4783/4783** Release-тестов, сборка без предупреждений и ошибок, Windows NativeAOT с шестью smoke-проверками и созданный NativeAOT файл Small/Classic/Crimson1458 загружен обоими серверами. Все три канонических размера Crimson сохраняют настоящие сердца после финализации. Ошибочная тестовая нагрузка CPU-бюджета исправлена: измеряет CPU потока вместо настенного времени; лимиты runtime и assertions не ослаблялись. Linux NativeAOT, прохождение настоящим клиентом и равенство всего прохода не проверены; стадия32 остаётся открытой.

WIP геометрии пещер Crimson (2026-09-08): обычный Crimson теперь получает собственные source-backed наклонный ствол, центральную камеру, ответвления, выемку входа и отложенные сердца вместо общего вертикального тоннеля в стиле Corruption. Девять прямых сравнений с официальной 1.4.5.8 совпали по всем нормализованным полям клеток, следующему RNG и упорядоченным координатам сердец, включая защищённые колонки и две пещеры с общей фазой размещения сердец. Это доказательство компонента геометрии, не всего прохода: поверхностная трава, алтари и геометрия Corruption остаются открытыми. Проверки текущего пакета записаны в agent memory; приёмка ниже относится к предыдущему блоку размещения.

Локальная приёмка ограниченного блока размещения/выемки evil-биомов: Release **4770/4770**, сборка без предупреждений и ошибок, Windows NativeAOT с шестью smoke-проверками и загрузка свежей Small/Classic/Corruption1458 в обоих серверах прошли. Пороги сравнения с прежним эталоном не менялись и пройдены: tile L1 `0.196269` (ранее `0.201211`), wall L1 `0.523785` (ранее `0.530400`). Сундуки всё ещё отличаются `181→144`; равенство мира и закрытие стадии не заявляются. Предыдущий зависший прогон тестов остановлен и записан в журнале; его причина не считается установленной. Linux NativeAOT и прохождение настоящим клиентом не проверены.

WIP размещения и выемки evil-биомов (2026-09-08): существующий проход Corruption теперь сканирует реальные поверхностные границы джунглей/снега и использует сохранённый прямоугольник пустыни, асимметричные source-броски краёв и ослабление исключений внутри каждого выбора. Подстановка фиксированной координаты удалена; исчерпание ограниченного поиска отменяет генерацию. Выемка пропасти защищает обычные/треснувшие кирпичи и стены данжа, снимает только активность, сохраняя остальные поля клетки; исключения для руды/сфер применяются только к Corruption, но не к Crimson. Прямые вызовы официального `CanEvilReplace` совпали во всех $553436$ известных сочетаниях материала/стены/активности; результат сохранён golden-регрессией. Это **не** закрывает всё ещё общую приближённую геометрию Corruption/Crimson, поверхностную траву, сердца/сферы/алтари или паритет мира целиком. Полная приёмка пакета отслеживается в agent memory; стадия остаётся открытой.

Полный аудит регистраций (2026-09-08): [все109 позиций и незакрытые parity targets](vanilla-worldgen-pass-audit.md). Финальные water/lava-броски теперь принадлежат Terrain; отдельно reseeded bridge TerrainLayers только переносит результат. Девять независимых официальных ordinary Terrain fixtures (три seed × Small/Medium/Large) совпали по всем type/frame/active клеткам, обеим liquid lines и следующему RNG. Очистка жидкости над песком теперь следует исходному surface scan вместо удаления embedded liquid по всей карте; отдельный официальный fixture совпал по всем600000 парам liquid amount/kind. Ни один результат не доказывает полный мир1:1 или точное позднее оседание жидкости.

Исправление обычных небесных островов (2026-09-08): существующий проход FloatingIslands вызывает SkyIsland1458 для CloudIsland/CloudLake. Десять прямых сравнений с официальным Linux1.4.5.8 на пяти seed совпадают по типам/активности блоков, стенам, количеству/типу жидкости и следующему RNG. Это геометрические фикстуры, не равенство целого мира по seed или косметических кадров. Исходные облачный низ, отдельная геометрия/глубина/дрейф озера, удержание воды, дождевые облака и RNG оформления стен заменяют приблизительные helpers и искусственную мелкую заливку. Выбор опорных координат использует исходный бюджет width-minus-one, строгие исключения spawn/spacing и поиск земли200..worldSurface без выдуманных fallback-координат. Особые seed, последующие дома/содержимое, визуальное равенство итогового мира и другие биомы остаются открыты. Вход данжа всё ещё упрощён: связный граф и успешная загрузка этот долг геометрии не закрывают.

Ванильный генератор переносится по проходам. `SourceBackedProvider1458` сохраняет прежние compatibility-проходы, добавляет нужные source-backed prerequisites и заменяет отдельные реализации под тем же идентификатором генератора.

Это не создаёт второй недоделанный «vanilla» и сохраняет compatibility-пути для специальных сидов, пока исходные алгоритмы Terraria переносятся по частям.

## Общий поток случайных чисел

`WorldGenerationRngMode.VanillaSharedRng` означает точный Terraria world-generation RNG API, общий для всей работы **внутри одного pass**. Закреплённый TerrariaServer 1.4.5.8 в `WorldGenerator.RunPass` создаёт `Main.rand = new UnifiedRandom(_seed)` перед каждым enabled pass, поэтому TerraRuntime начинает каждый vanilla-shared pass из его локального seed и сохраняет порядок вызовов только внутри pass. Перенос состояния между зарегистрированными passes был бы ошибкой совместимости; так же, как и параллелизация RNG-sensitive работы внутри pass.

## Source-backed bootstrap Reset

Для обычных сидов, а также чистого профиля `Don't Dig Up`/Remix, на трёх канонических размерах Terraria план начинается с `terraria:1.4.5.8/Reset`. Bootstrap расходует RNG, который Terraria использует до Terrain, и сохраняет состояние для последующих проходов, включая:

- сторону и координату dungeon;
- origins jungle и snow;
- случайные левую и правую границы пляжей;
- выбранные ore tiers;
- tree styles и позиции переходов;
- cave и surface background styles;
- moon style и часть начального состояния нового мира;
- world-size-dependent generation counts.

Ключевая конфигурация пляжей закреплена по source: `BeachBordersWidth = 275`, `BeachSandRandomCenter = 320`, `BeachSandRandomWidthRange = 20`, `BeachSandDungeonExtraWidth = 40`, `BeachSandJungleExtraWidth = 20`.

Для контроля расхода RNG добавлен фиксированный checkpoint. Для seed `1458` в обычном малом мире $4200 \times 1200$ Reset должен получить границы пляжей `322 / 3830`, dungeon side `-1`, dungeon location `484`, а следующим значением общего RNG после Reset должно быть `289143048`.

`tools/ci/probe_worldgen_reset.py` декомпилирует закреплённый официальный TerrariaServer 1.4.5.8 `WorldGen.Reset` и отклоняет изменения проверенных Reset-констант, построения случайных границ пляжей и порядка tree/cave initialization. Отдельный workflow `Terraria Worldgen Reset Contract` запускает этот source-contract вместе с профильными тестами реализации.

Чистая Remix-ветка намеренно мала и закреплена по source: `Reset` заменяет hell-chest item `112` на `683` и выбирает jungle origin из диапазона $20\%$–$35\%$ вместо обычных $15\%$–$30\%$. Zenith не допускается только потому, что включает Remix: он также активирует другие special-ветви, ещё не перенесённые в source-backed виде.

## Source-backed Terrain

`terraria:1.4.5.8/Terrain` source-backed для обычных сидов и чистого профиля `Don't Dig Up`/Remix на настоящих размерах Terraria:

| Размер | Тайлы |
| --- | ---: |
| Малый | $4200 \times 1200$ |
| Средний | $6400 \times 1800$ |
| Большой | $8400 \times 2400$ |

Перенесены state machine форм рельефа, заполнение колонок Dirt/Stone, история поверхности и её ретаргетинг у берега, квантование rock layer шагом в 6 тайлов и `FlatBeachPadding = 5`. Terrain теперь получает случайные границы пляжей от предыдущего Reset bootstrap вместо фиксированной compatibility-константы. Для чистого Remix он также использует Terraria alternate surface-offset distribution и глубокую инициализацию/потолок rock layer.

Нестандартные размеры, Zenith, комбинации special switches и secret switches пока используют старый compatibility Terrain. Их ветви Reset ещё не объявляются source-exact, поэтому source-backed Reset намеренно не расходует дополнительный RNG в этих compatibility-сценариях. После pure Remix Terrain все последующие source-shaped overlays также остаются на compatibility-пути: это ограниченный slice `Reset + Terrain`, а не полный Remix world parity.

## Метаданные

Source-backed Terrain сохраняет рассчитанные `worldSurface` и `rockLayer` в metadata workspace. Compatibility Metadata по-прежнему рассчитывает spawn, dungeon anchor и сохраняет профиль сида, после чего source-backed значения слоёв восстанавливаются.

Для source-backed ordinary и pure Remix Terrain миров Reset bootstrap теперь также переносится в `RuntimeWorldGenerationMetadataSnapshot`. Fresh `.wld` persistence записывает полученные Reset значения moon type, tree/cave transition positions и styles, primary/secondary background styles, cloud timer/count, wind, slime-rain countdown и pre-hardmode ore choices. `flat` и custom generators не имеют такого bootstrap и продолжают использовать консервативные defaults нового мира.

Этот persistence bridge нужен следующим проходам: Jungle, desert, ocean, structures и decoration смогут использовать один и тот же результат Reset во время генерации, а сохранённый мир после restart не откатит эти исходные выборы обратно к compatibility defaults.

## Формирование поверхности в Smooth World

Обычный канонический путь теперь выполняет clean-room реализацию `Smooth World` из TerrariaServer 1.4.5.8 вместо прежней эвристики, зависевшей от координат. Pass сохраняет два source-ordered сканирования внутри границы $20\,\text{тайлов}$, точки расхода общего RNG, эрозию открытых краёв и заполнение диагональных разрывов, все четыре направления slope, half-bricks, нормализацию семейства песка через `SmoothSlope` и исправление slope без опоры.

Представление формы сделано явным: `TileShape1458` владеет runtime-отображением полного блока, half-brick и четырёх vanilla slope. Отдельный `WorldSmoothingCatalog1458` владеет закреплёнными для версии tile capabilities: очисткой при генерации, запретом slope, исключениями hammer/pound, защитой опор под объектами, семейством песка и временной solidity треснувших кирпичей. Поэтому topology-код не содержит анонимных цепочек tile identity или чисел формата хранения.

Focused fixtures проверяют точный порядок вызовов RNG и каждое семейство мутаций, включая верхние и нижние направления slope. Каноническая Small integration-проверка требует наличия всех пяти неполных форм в собранном мире. `tools/ci/probe_worldgen_smooth_world.py` независимо сравнивает runtime capability sets и decision routes с закреплённым decompile; `.github/workflows/terraria-worldgen-smooth-world.yml` заново получает это доказательство из официального бинарника с закреплённым SHA-256.

Этим закрыт аудит обычного shape writer `Smooth World`. Это не означает byte-identical vanilla world: силуэт terrain, точная upstream-геометрия проходов, dungeon, ocean alignment и shaping специальных сидов остаются отдельными границами parity.

## Рельеф обычного Ада

В существующем обычном каноническом проходе `Underworld` сплошная нижняя пепельная плита и периодические лужи заменены source-порядком: вырезание потолка, изменение глубины лавовых бассейнов, вертикальные/горизонтальные пепельные проходы, жидкостные полости и количество залежей адской руды пропорционально площади. Существующие владельцы растительности, крепостей, освещения, мебели и декораций сохранены. Малые проходы учитывают защищённые/frame-important плитки, уровни жидкостей, форму блоков и точные RNG-вызовы, включая перезаписанные выборки скорости. Неподдержанные типы и размеры проходов отклоняются.

Проход переиспользует владельца физики жидкости для `QuickWater` до генерации данжа, с сохранённым уровнем воды Terrain, а не значением только для загрузки. Генерационное окружение использует исходную позицию шиммера Reset до Aether и source-очистку взаимодействий жидкостей, включая асимметрию шиммера сверху. Загрузка остаётся отдельным состоянием жизненного цикла, а не вторым алгоритмом жидкости.

Независимое сравнение с официальным сервером охватывает 21 сценарий малых проходов без расхождений плиток или следующего RNG; семь результатов сохранены в golden-регрессиях. Проверки канонических размеров на границе прохода также отклоняют прежний сплошной нижний ряд. Это не заявление о равенстве целого мира или специальных сидов.

Оба поздних этапа оседания теперь включают очистку жидкости из твёрдых блоков по `WorldGen.WaterCheck`, с генерационными исключениями временной проницаемости. Пропуск стал наблюдаемым после того, как поток жидкости в Underworld начал перемещать её по всей карте. Валидатор остаётся строгим. Полное соответствие позднего оседания открыто: существующие вертикальные проходы и уплотнение столбцов не реализуют полную повторяющуюся последовательность `QuickWater / WaterCheck / quickSettle`, правила после Dungeon/Aether и все побочные эффекты объектов.

## Проверка результата

`.github/workflows/terraria-vanilla-generated-world-acceptance.yml` для малого канонического мира:

1. собирает TerraRuntime и запускает профильные тесты worldgen;
2. создаёт настоящий `.wld` генератором `terraruntime:vanilla`;
3. загружает файл собственным verifier TerraRuntime;
4. запускает закреплённый TerrariaServer 1.4.5.8 с этим миром и требует открытия игрового порта.

Существующая проверка `flat` не изменяется.

## Что ещё не ванильное

Для обычного мира source-backed уже стали Reset и Terrain, а для pure Remix добавлена ограниченная ветка, но в provider всё ещё остаются compatibility-реализации биомов, пещер, руд, dungeon и поздних модификаторов специальных сидов. В закреплённом каталоге Terraria 1.4.5.8 зарегистрировано 109 проходов. Настоящий ordinary или Remix parity требует заменить эти compatibility-группы source-backed последовательностями passes и затем добавить reference-world comparison, а не наращивать новые приблизительные эвристики.

name: Sign Runtime Apply

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/sign-runtime-apply.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 11.0.x
      - name: Apply production sign wiring
        run: python3 .github/scripts/apply_sign_runtime.py
      - name: Restore
        run: dotnet restore TerraRuntime.slnx
      - name: Test
        run: dotnet test TerraRuntime.slnx -c Release --no-restore
      - name: Commit tested wiring to main
        shell: bash
        run: |
          set -euo pipefail
          if git diff --quiet -- src/TerraRuntime/TerrariaServerHost.cs; then
            echo "No source changes to commit."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/TerraRuntime/TerrariaServerHost.cs
          git commit -m "gameplay: wire authoritative signs into production"
          git fetch origin main
          git rebase origin/main
          git push origin HEAD:main
